# CLAUDE.md

이 파일은 Claude Code가 claire_bible 저장소에서 작업할 때 따라야 할 지침이다.

## 프로젝트 개요

텔레그램으로 던진 링크/문서/키워드를 스크랩 → Gemini로 구조화 → 팔란티어식 타입 온톨로지 그래프로 적재하고, 검색 시 LLM이 정리해주는 개인 지식베이스. 상세 비전/설계/마일스톤은 `GOALS.md`·`PLAN.md`, 진행 상태는 최신 `TODO_2026-*.md`를 참조.

## 작업 방식(사용자 협약)

- **피드백은 먼저 문서로 정리한 뒤에 처리한다.** 사용자가 버그/개선 요청을 구두로 말하면 즉시 코드부터 고치지 말고, 먼저 저장소 루트의 `TODO_<날짜>.md`(또는 해당 날짜 파일이 이미 있으면 거기에 추가)에 항목별로 정리(원인 파악 전이면 증상만이라도)한 뒤 그 문서를 기준으로 하나씩 처리한다. 여러 건이 한 번에 들어올 때 특히 중요 — 항목이 누락되거나 뒤섞이는 것을 방지.
- **검증과 재현**: 결과는 재현 가능하게. 위조·과장 보고 절대 금지. 추측은 "추측"이라 명시.
- **정직**: 안 되면 정직하게 보고하고 도움 요청. 결과 위조 금지.
- **데이터 보존**: 데이터 임의 삭제 절대 금지. 신중히 다룰 것(특히 raw 쿼리/조사 데이터는 알고리즘 변경 시 재적재를 위해 모두 보관).
- **단계적**: ad-hoc으로 때우지 말 것. 차근차근 단계적으로. 끝을 보고 올 것(중간에 방치 금지).
- **모듈화**: 동일 코드 반복 입력 금지. 모듈화 습관화.
- **advisor**: 의사결정·방법론·구현 선택은 advisor에 적극 자문하되, 방향이 맞는지 비판적으로 검토하고 어긋나면 바로잡을 것.
- **본질 집중 + 한 발 물러서 크게 보기**. 길 잃으면 멈추고 질문.
- **보고는 한국어**.

## 환경 주의

- Bash/ssh 출력이 자주 잘리므로 긴 출력은 임시 파일에 쓰고 Read로 확인.
- 로컬 `serve-api` 등 소켓 바인드 백그라운드 프로세스는 sandbox가 SIGTERM(144)으로 죽인다 — `run_in_background:true` + `dangerouslyDisableSandbox:true` 필요.
- 컨테이너 코드 즉시 반영은 `docker cp`(임시, 재시작 시 소실). 영구 반영은 `deploy.sh` 재빌드.
- 병렬 tool 호출 묶음에서 1개가 실패하면 나머지가 cancel될 수 있으니 위험한 작업은 순차 실행.
- 원격 배포: `ssh -p 2222 blackan@192.168.1.8`, 저장소 `~/claire_bible`, docker compose 6컨테이너(bot/api/refresh/recover/backup/expand). 배포는 `deploy.sh`(CI 게이트 통과 필요, `SKIP_CI=1`로 우회 가능하나 지양).
