# 2026-07-21 종합기능 개선 + 모바일 UI 4건 — TODO

사용자가 구두로 전달한 항목들을 먼저 정리. 쉬운 것(모바일 UI)부터 처리하고
어려운 것(종합기능 재설계)은 조사 후 별도 논의.

## 1. 종합(synthesis) 기능이 기대와 다름 — 재설계 필요 (난이도: 높음)

**사용자 증상(원문)**: "종합기능이 별로임. 원했던것은 종합에 추가된 내용을
바탕으로 조사계획을 세워서 웹으로 조사해서 고품질의 문서를 생성한 뒤에
적재하는 거였음"

**현재 상태(조사 완료, 2026-07-21)**: 현재 "종합" 기능은 선택한 노드들을 모아
`provider.summarize_search()`로 요약 답변 하나를 만들어 `<div class=synth>`로
보여주는 게 전부(`graphview.py` `synthesis_context()`/`synthesize()`, 프론트
`synth()` — 대략 1870번대 줄, API `POST /synthesize`). **결과 렌더링에 추가
조사(웹검색) 버튼/입력창이 전혀 없어 여기서 흐름이 끊김.**

한편 `src/claire/expand/research.py`(`contextual_research()`/`build_context()`)에
이미 "맥락 구성→`provider.research()`(웹검색 grounding)→`judge_research()`
게이트→통과시 ingest"라는 조사 파이프라인이 존재하고, API `POST /research`도
있음 — 단 지금은 `node_id`/`doc_id` 딱 1개 기준으로만 맥락을 구성함(엔티티
리스트나 종합 결과 텍스트는 못 받음). 프론트의 "🔬 더 알아보기"(`doResearch()`)
버튼도 노드 상세 패널에만 있고 종합 결과 화면과는 무관.

사용자가 원하는 것:
1. 종합 결과(또는 종합에 모은 노드들)를 바탕으로 **조사 계획**을 세우고
2. 그 계획에 따라 **웹으로 추가 조사**를 수행하고
3. 그 결과로 **고품질 문서를 새로 생성**해서
4. 그래프에 **적재**까지 하는 파이프라인.

**연결 방향(초안, 확정 아님 — advisor 자문 + 사용자 확인 필요)**:
(a) `research.py`의 `build_context()`가 `entity_ids: list[str]`도 받도록
    확장 — 이미 있는 `synthesis_context()`가 리스트 기반이라 재사용 용이.
(b) `research_route`(`POST /research`)가 `entity_ids` 필드를 받아 넘기도록
    확장.
(c) `synth()`의 결과 렌더링에 `doResearch`류 입력창+버튼을 추가해, 지금
    종합에 모아둔 노드 id 집합(`synthSet`)을 그대로 조사 컨텍스트로 넘김.
(d) "조사 계획을 세운다"는 부분 — 지금 `research()`는 바로 웹검색으로
    들어가는 단일 단계인데, 사용자가 원하는 건 종합 내용을 먼저 분석해
    "무엇을 조사할지" 계획(하위 질문 목록 등)을 LLM이 먼저 세우고, 그 계획별로
    조사하는 다단계 구조로 보임 — 이 부분은 기존 코드에 없어 새로 설계해야 함.

- [x] 설계 확정(플랜 모드, 사용자 확인) — 사용자가 명시적으로 정한 2가지:
      ① "계획 먼저 보여주고 확인"(원클릭 전자동 아님) ② 하위질문 기본 3개,
      `.env`(`CLAIRE_RESEARCH_PLAN_MAX`)로 조정 가능. 상세 설계는
      `/home/blackan/.claude/plans/ethereal-wobbling-heron.md` 참고.
- [x] **구현 완료**:
      - `config.py`: `CLAIRE_RESEARCH_PLAN_MAX`(기본 3) 추가.
      - `provider.py`: `PlannedQuestion`/`ResearchPlan`/`ComposedDocument` 스키마 +
        `MockProvider.plan_research()`/`compose_document()` 결정론적 stub.
      - `gemini_provider.py`: 실제 `plan_research()`/`compose_document()` —
        `classify_watch()`와 동일한 구조화출력(`response_schema`) 패턴, `_call()`
        경유(rate-limit 회복 자동 적용).
      - `expand/research.py`: `_ingest_report_doc()` 파라미터화(source_type/
        source/title, 기존 호출부 무변경) + 신규
        `plan_research_from_synthesis()`(단발 LLM, 계획만) +
        `run_planned_research()`(질문별 조사+게이트→통과분 합성→적재, 일부 탈락
        해도 통과분으로 진행, 전부 탈락하면 실패).
      - `api/server.py`: `POST /synthesize/plan`(JSON), `POST /synthesize/research`
        (NDJSON 스트림, `/research`와 동일 골격) 추가.
      - `graphview.py`: 종합 결과 아래 "🧭 조사계획 세우기" 버튼 → 체크박스 계획
        목록("이대로 조사 시작"/"취소") → NDJSON 진행 로그 → 합성 문서+적재 결과
        +"문서 보기" 링크. `READONLY` 세션은 버튼 자체 미노출(기존 관례와 동일).
- [x] **테스트**: `tests/test_research.py`에 9건 추가(계획 기본/env override,
      전부통과 적재, 일부 게이트탈락, 전부탈락 실패, provider 계약, 인자누락,
      progress 이벤트 순서). `pytest tests/`: **214건 + 서브테스트 6건 전체 통과**
      (기존 205건 + 신규 9건).
- [x] **실제 렌더링 검증**: 로컬 mock DB + `serve-api`(`CLAIRE_PROVIDER=mock`) +
      Playwright(Chromium, 실제 클릭)로 종합→조사계획 버튼→체크박스 3개
      노출→"취소"(재요청 없이 종합화면 복귀 확인)→조사 시작→NDJSON 진행→
      "질문 3개 중 통과 3개·✅ 그래프에 추가됨"→합성 문서 렌더→그래프 신규
      노드(5/5→6/6)→좌측 문서목록에 `synthesis_research` 문서 등장→"문서 보기"
      클릭→문서 패널 정상 이동(그래프 노드 강조, "이 문서의 노드" 목록 포함)까지
      전 구간 콘솔 에러 없이 확인.

- [x] **실 Gemini 확인(부분)**: 사용자 승인 받아 실제 키로 로컬 mock-DB(테스트
      픽스처 데이터, 가짜 도메인 `seed.example/*`)에 대해 1회 실행.
      - 종합(synthesize) 실제 LLM 호출 정상 — 두 문서 내용 기반 합리적 답변.
      - `plan_research()` 구조화 출력(JSON) 파싱 정상 — 프롬프트 의도대로
        "종합 답변에 없는 빈틈"(신뢰성·주제·최신성)을 겨냥한 하위질문 3개를
        실제로 생성, 반복 질문 없음(설계 의도 그대로 작동).
      - 게이트/실패 처리 실전 확인 — 3개 질문 전부 `research()`가 정직하게
        INSUFFICIENT(신뢰할 자료 못 찾음) 반환(가짜 도메인이라 당연), "전부
        탈락 시 실패 반환" 경로 정확히 트리거.
      - **한계(정직히 기록)**: 테스트 픽스처 데이터라 "게이트 통과→문서합성→
        적재"까지 가는 해피패스는 이번엔 실제로 확인 못 함 — 버그 아니라
        환각방지가 의도대로 작동한 결과지만, 성공 경로 자체의 실기(real)
        검증은 아직 없음.
- [x] **해피패스 실 Gemini 검증 여부 — 사용자 결정**: 원격 프로덕션 DB 사본으로
      추가 검증할지 물었고, 사용자가 "여기서 멈추기"(권장안) 선택 — mock 기반
      해피패스 검증(pytest 9건 + Playwright 클릭스루) 및 위 실 Gemini 배선
      검증으로 충분하다고 판단, 실제 사용하면서 확인하는 쪽으로.

## 핸드오프

- [x] **커밋**: `2c5404a` — 모바일 UI 4건 + 종합→조사 파이프라인 전체.
- [x] **배포**: CI 게이트(`scripts/ci.sh`, 214건+서브테스트6) 통과 → `./deploy.sh`
      실행 → 6컨테이너(bot/api/refresh/recover/backup/expand) 전부 재빌드+재기동.
      배포 후 헬스체크(`/health`) 확인: `ok:true, degraded:false, db:ok`,
      문서 216·엔티티 594·관계 654, 최근 백업 정상.

이번 세션 필수 작업 전부 완료(문서화→쉬운순 4건 UI 수정→플랜모드 설계→종합
파이프라인 구현→테스트→실 Gemini 확인→커밋→배포→헬스체크). 다음 세션 이어받을
필수 항목 없음.

## 2. PC 브라우저 우측 녹색 버튼 2개 — [x] 원인 확정 + 수정 완료

**사용자 증상(원문)**: "피씨 브라우저로 접속하면 우측에 별의미없는 녹색버튼이
2개 생김"

- [x] **원인**: `#panelclose`(✕)/`#panelpeek`(◂)는 모바일 전용 오버레이 버튼인데
      데스크톱(`@media` 밖) 기본값에 `display:none`이 빠져 있었음. 그 결과 전역
      버튼 기본색(녹색, `--btn-bg`)을 그대로 뒤집어쓴 채 `#wrap`의 평범한 flex
      자식으로 노출 — 하나는 작은 사각형, 하나는 `align-items:stretch`로 세로로
      늘어난 초록 스트립으로 보였음(`src/claire/graphview.py` `#panel` 규칙
      바로 아래).
- [x] **수정**: `#panelclose,#panelpeek{display:none}`을 데스크톱 기본 규칙에
      추가. 모바일 미디어쿼리의 `body.peek/reading` 조건부 표시는 그대로 유지.
- [x] **검증**: 로컬 mock DB로 `serve-api` 띄운 뒤 Playwright(1440×900)로
      실제 렌더링 — 수정 전 스크린샷에서 우측 상단 초록 사각형 1개 +
      세로로 늘어난 초록 스트립 1개 확인, 수정 후 스크린샷에서 완전히 사라짐.
      모바일 peek/reading 상태에서 ✕/◂ 버튼이 여전히 정상 표시·동작하는지도
      재확인(회귀 없음).

## 3. 폰 "크게읽기" 제목이 버튼들과 폭을 공유해 좁아짐 — [x] 수정 완료

**사용자 증상(원문)**: "폰에서 크게읽기로 들어가면, 제목과 우측에 버튼들이
나열되는데, 버튼과 제목이 너비를 공유하니 제목이 매우 좁아지는 문제 발생.
분리해야함"

- [x] **원인**: `#reader .rhead`가 `flex-wrap` 없이 제목(`h1{flex:1}`)+
      A−/A+/🔗/✕ 4개 버튼을 한 줄에 배치 — 좁은 화면에서 버튼들(~200px)+
      패딩을 뺀 나머지가 제목 폭이 되어 심하게 좁아짐.
- [x] **수정**: 모바일 미디어쿼리에 `#reader .rhead{flex-wrap:wrap}` +
      `h1{flex:0 0 100%}`(제목이 첫 줄 전체 차지) + `.rzoom{margin-left:auto}`
      (버튼 줄은 둘째 줄에서 우측 정렬) 추가.
- [x] **검증**: Playwright 모바일 에뮬레이션(390×844, iPhone 13 뷰포트)으로
      문서 하나를 열어 크게읽기 진입 → 스크린샷으로 제목이 첫 줄 전체 폭을
      쓰고 버튼 4개가 둘째 줄에 우측 정렬로 분리된 것 확인.

## 4. 폰 하단 문서리스트 즐찾/전체 이중노출 — [x] 탭으로 분리 완료

**사용자 증상(원문)**: "폰에서 아래 문서리스트를 슬라이드 업 하면 즐찾/문서
리스트 2개가 이중으로 나와서 좁습니다. 즐찾과 리스트를 탭으로 분리하면
좋겠음"

- [x] **구현**: `#docs`에 `#doctabs`(📄 전체 / ⭐ 즐겨찾기 버튼 2개) 추가,
      `setDocTab(tab)` JS로 `#docs`에 `.tab-pinned` 클래스 토글. 모바일
      미디어쿼리에서 기존 4:6 분할 규칙을 제거하고 활성 탭 쪽만
      `display:block`+`flex:1`로 전체 공간을 쓰게, 비활성 탭은
      `display:none`으로 변경. 즐찾이 비어있으면(`haspinned` 없음) 탭 자체를
      숨기고 `#doclist`가 전체를 씀(기존 동작 유지). 검색으로 즐찾 결과가
      0건이 되면 자동으로 "전체" 탭으로 리셋(`renderDocs()`에서 처리).
      기존 `#pinnedhead`("⭐ 즐겨찾기" 라벨)는 탭 라벨과 중복이라 모바일
      탭 모드에서는 계속 숨김 처리.
- [x] **검증**: Playwright 모바일 에뮬레이션으로 listopen 상태 스크린샷 —
      "전체" 탭에서 즐찾 아닌 문서 3건이 전체 화면을 씀, "즐겨찾기" 탭
      클릭 시 즐찾 문서 2건이 전체 화면을 쓰며 전환. 콘솔 에러 없음.

## 5. 폰 바텀시트 드래그 핸들(끌게) 없음 — [x] 추가 완료

**사용자 증상(원문)**: "폰에서 아래 문서리스트를 슬라이드 업 하면 다시 내릴
수 있음을 알 수 있게 잡아서 끌 수 있는 끌게가 리스트 레이아웃 상단에 있으면
좋겠음"

- [x] **구현**: `#docs` 최상단(`.dhead` 검색창 위)에 `#draghandle`(막대 pill
      모양, `::before`로 그림) 추가. `attachDragSheet(el)`가 `#docs` 엘리먼트
      전체에 걸려있어 핸들 자체엔 별도 이벤트 리스너 불필요 — 순수 시각적
      어포던스. 데스크톱에서는 `display:none`으로 숨김.
- [x] **검증**: Playwright 모바일 에뮬레이션 스크린샷에서 리스트 상단에
      회색 막대 핸들 정상 노출 확인.

## 진행 상태 요약

2~5번 전부 로컬 mock DB(`data/claire.db` 사본) + `serve-api` +
Playwright(Chromium, 실제 렌더링)로 코드 수정 → 실제 화면 확인까지 완료.
`pytest tests/`(205건+서브테스트 6건) 전체 통과, 회귀 없음. **배포는 아직
안 함** — 사용자 확인 후 진행.

1번(종합→조사 파이프라인 재설계)은 설계 논의가 필요해 보류 중 — 아래 참고.
