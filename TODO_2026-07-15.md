# 2026-07-15 모바일 UI 버그 TODO

사용자가 오늘 발견한 버그/개선 2건. 상태: `[ ]` 미착수 · `[~]` 진행중 ·
`[x]` 해결(배포/검증 완료).

## 1. 모바일에서 글(문서) 선택 시 화면 레이아웃 완전히 깨짐 — 글자 겹침/오버플로우

**사용자 증상**: 배포된 걸 모바일로 봤는데 글(문서) 선택 시 화면이 글자들이 오버플로우 나고
레이아웃이 완전히 망가짐.

**진행 메모(정정)**: 본래 협약상 "구두 피드백 → 먼저 문서화 → 처리" 순서인데, 이번엔 조사가
빠르게 원인까지 이어져 문서화 전에 수정·배포까지 진행했다. 사후 기록.

- [x] **원인 1(주범, 확정)**: `#pinnedlist`(즐겨찾기 목록)의 `max-height:32%`가 모바일에서
      전혀 적용되지 않는 CSS 퍼센트-높이 버그. `max-height:32%`는 부모(`#docs`)가 **명시적
      `height`**를 가질 때만 퍼센트가 계산되는데, 모바일 미디어쿼리(`@media max-width:820px`)는
      `#docs`에 `max-height:34vh`만 주고 `height`는 안 준다 — CSS 스펙상 이 경우 퍼센트는
      `none` 취급되어 무시됨. 데스크톱은 `#wrap`이 `flex-direction:row`라 `#docs`가 flex
      stretch로 실제 `height`를 가져 우연히 정상 동작했음(모바일 전용 회귀).
      실사용자 계정 즐겨찾기 7개 → `#pinnedlist`가 자기 콘텐츠 높이(약 580px)만큼 무제한으로
      부풀어 `#docs` 박스를 뚫고 나가 `#doclist`·우측 `#panel`(문서 상세)을 완전히 뒤덮음
      → 글자가 겹쳐 보이는 현상.
      실사이트(`claire.blackan.net`, readonly 세션)에서 실측 재현: `#panel` top=347px,
      `#doclist` bottom=843px로 496px 겹침 확인.
      **수정**: `src/claire/graphview.py` 모바일 미디어쿼리에 `#pinnedlist{max-height:11vh}`
      명시 추가(데스크톱 의도였던 "34vh의 32%"를 절대 vh 값으로 재지정).
- [x] **원인 2(부수, 확정)**: 문서 상세 패널의 요약 박스(`.synth`, `renderDocPanel`)에
      `word-break`/`overflow-wrap`이 없어, 요약에 공백 없는 긴 토큰(URL 등)이 섞이면 패널
      폭을 넘어 가로로 삐져나감. `.md`(읽기 팝업)·`.docitem b`(목록 제목)는 예전에 비슷한
      신고로 개별 수정됐었는데 `.synth`는 그때 안 걸렸음.
      **수정**: 클래스별 개별 패치 대신 `#panel` 컨테이너에 `word-break:break-word` 추가
      (상속 속성이라 `.synth`·관찰(`li`)·출처(`.doc p`)·병합출처(`.srclist`) 전부 한 번에 커버).
- [x] **검증**: 로컬 mock 서버(즐겨찾기 7개 + 긴 URL 요약 시딩) + Playwright 모바일
      뷰포트(390×844)로 재현·수정 확인. `pytest tests/test_graphview.py`(12건) +
      `scripts/ci.sh` 전체(205건) 통과.
- [x] **배포**: `./deploy.sh` (CI 게이트 통과) 완료. 배포 후 실사이트에서 동일 문서
      재확인 — 겹침 해소, 병합출처 링크도 박스 안에서 정상 줄바꿈.

**남은 것**: 없음. 확인용으로 원격 DB에 새 `readonly`(`/webro`) 세션을 하나 발급했음(소유자
세션은 안 건드림) — 필요 없으면 다음 `/webro` 재발급 시 자동 대체됨(scope별 단일 세션).

## 2. 폰에서 글 읽는 영역이 그래프에 비해 너무 좁음

**사용자 증상**: "글 읽을때 영역이 그래프에 비해 너무 좁은듯."

**1차 오진(정정)**: 처음엔 데스크톱(`#panel width:360px` 고정 vs `#netwrap flex:1`)
문제로 판단해 `#panel{width:clamp(380px,30vw,640px)}`로 수정했으나, 사용자가 "폰
화면 얘기였다"고 정정 — **되돌림**(`#panel{width:360px}` 원복, 데스크톱은 그대로).

**진짜 원인**: 모바일 미디어쿼리(`@media max-width:820px`)에서 그래프(`#netwrap`)가
`height:58vh`로 **항상 고정** — 문서/노드를 클릭해 우측(모바일에선 아래) 패널에 실제
읽을 내용이 떠도 그래프가 화면 절반 이상을 계속 차지해, 정작 읽는 영역이 좁아 보임.

- [x] **수정**: `#panel`이 기본 힌트가 아닌 실제 내용으로 채워지면(`body.reading`,
      `syncReadingState()` — `#panel`을 `MutationObserver`로 관찰해 판단, 특정 호출부에
      의존하지 않아 새 패널-채우기 경로가 생겨도 자동 커버) 모바일 전용으로
      `#netwrap{height:22vh;min-height:160px}`로 접어 패널·문서목록에 세로 공간을
      돌려준다. 탐색(기본 힌트) 중엔 원래 58vh 유지. vis-network 캔버스는 기존
      `ResizeObserver`(`relayout()`)가 크기 변화를 자동 감지해 재렌더 — 추가 코드 불필요.
- [x] **검증**: 로컬 mock 서버 + Playwright 모바일 뷰포트(390×844)로 탐색→읽기→탐색
      왕복 확인(그래프 높이 489.5px ↔ 185.7px, `body.reading` 토글 정확). `pytest
      tests/test_graphview.py`(12건) 통과.
- [x] **배포**: `./deploy.sh` (CI 게이트 통과) 완료.

## 3. 스크롤하거나 그래프를 조작하다 보면 문서 목록 영역이 투명해짐

**사용자 증상**: "아직도 스크롤 하다보면 아니면 그래프 돌아다니다보면, 글 리스트 영역이
투명해지는 버그가 있다."

**조사(Chromium 로 재현 시도, 실패)**: 그래프 캔버스를 드래그(팬)하는 동안 `#docs`의
`getComputedStyle().opacity`/`backgroundColor`를 92프레임 연속 샘플링 — **한 번도 안
바뀜**(opacity 항상 `1`, 배경색 고정). JS/CSS 로직이 투명도를 바꾸는 코드는 없음(전체
파일에 `#docs`·`#doclist`에 대한 opacity/style 조작 코드 자체가 없음을 확인).
→ 코드 버그가 아니라 **캔버스(vis.js)가 계속 다시 그려지는 동안 인접 요소가 GPU
합성 과정에서 순간적으로 페인트가 빠지는 렌더링 글리치**로 추정(iOS Safari 등에서
"바쁜 `<canvas>` 옆 형제 요소가 스크롤/팬 중 잠깐 안 그려짐"은 잘 알려진 부류의 버그).
Playwright 는 Chromium 기반이라 이 종류의 WebKit 컴포지팅 버그를 재현하지 못함 —
**원인 미확정, 가설 기반 조치**.

- [x] **조치(가설)**: `#docs`·`#panel`(그래프와 나란히 있고 자체 스크롤도 있는 두 영역)에
      `transform:translateZ(0)`로 별도 GPU 레이어 승격 — 캔버스 리페인트로부터 페인트
      경계를 분리하는, 이 부류 버그의 표준 완화책. 레이아웃/동작 변경 없는 안전한
      추가라 부작용 리스크 낮음.
- [x] **회귀 확인**: 로컬 mock 서버 + Playwright 로 레이아웃/버튼 위치/배경색 이상 없음
      확인(스크린샷). `pytest tests/test_graphview.py`(12건) 통과.
- [x] **배포**: `./deploy.sh` (CI 게이트 통과) 완료.
- [x] **실기기 검증**: 사용자가 스크린샷으로 재현 제공(사이트 로딩 직후 즐겨찾기 첫 글
      클릭) — **가설이 틀렸음이 밝혀짐**. GPU 합성 글리치가 아니라 **순수 CSS 스태킹
      버그**였다.

**진짜 원인(확정, Playwright 로 실측 재현 성공)**: `#netwrap`이 `overflow:visible`인
채로 `position:relative`였다. vis-network 캔버스는 자체적으로 `position:relative`를
쓰는데(내부 오버레이용), **"positioned 요소는 z-index:auto 라도 같은 스태킹
레벨에서 non-positioned 형제 위에 그려진다"**는 CSS 규칙 때문에, 캔버스 크기가
`#netwrap` 박스보다 커지는 순간(리사이즈 relayout() 타이밍이 늦어지는 등) 그
오버플로우가 무조건 `#docs`(문서목록, 원래 non-positioned) 위에 그대로 그려졌다.
`canvas.style.height` 를 강제로 키워 Playwright 로 100% 재현 확인 — 정확히 사용자
스크린샷과 같은 양상(그래프 텍스트가 즐겨찾기·문서목록 위에 겹쳐 보임 — "투명해짐"은
실제로는 그래프가 위에 덮어 그려진 것). 지난 `transform:translateZ(0)` 조치는 이
메커니즘을 안 건드려서 효과가 없었다(제거는 안 함 — 무해하고 다른 부류의 문제엔
도움될 수 있음, 그대로 유지).
（재현 시행착오 기록: 초반엔 반복 테스트로 오염된 같은 페이지에서 극심한 재현이
나와 다른 메커니즘으로 오인할 뻔했으나, 격리된 새 브라우저 컨텍스트로 재검증해
정확한 인과관계를 확인함.）

- [x] **수정**: `#netwrap`에 `overflow:hidden` 추가 — 뷰포트 역할의 컨테이너는 자기
      콘텐츠를 항상 잘라내는 게 맞다는 원칙으로, 원인(타이밍 레이스)을 정확히 안 잡아도
      구조적으로 이 부류의 새어나감 자체를 봉쇄.
- [x] **검증**: 격리된 브라우저 컨텍스트에서 캔버스를 600px로 강제로 부풀려도 전혀
      새어나가지 않음을 스크린샷으로 확인. `pytest tests/test_graphview.py`(12건) 통과.
- [x] **배포**: `./deploy.sh` (CI 게이트 통과) 완료.

## 4. 문서 선택 후 해제해도 그래프 영역이 원래 크기로 안 돌아옴

**사용자 증상**: "문서를 선택했다가 다시 선택해서 사라지게 했는데 그래프영역 복구가
안됐다." 스크린샷: 그래프 캔버스는 위쪽 일부에만 작게 남아있고, 그 아래로 빈 공백이
크게 있다가 `+/−` 줌 버튼과 문서목록이 훨씬 아래에 위치 — `#netwrap` 박스 자체는
원래 크기(58vh)로 돌아갔는데 vis-network 캔버스 내부 렌더 영역만 이전(읽기모드,
22vh) 크기에 갇혀 안 늘어난 모양.

**원인(확정, 코드 추적)**: `relayout()`은 `netBusy`가 `true`면 아무것도 안 하고
스킵한다(`function relayout(){ if(!net || netBusy) return; ...}`). `fit()`/`moveTo()`
경로는 `markBusy(700)`로 안전 타임아웃이 걸려있어 완료 이벤트가 안 와도 700ms 뒤
자동 해제되지만, **`dragStart`/`dragEnd`만 타임아웃 안전망 없이 서로에게만
의존**(`dragStart`→`netBusy=true`, `dragEnd`→`netBusy=false`)하고 있었다. 그런데 이
앱은 모바일에서 세로 스와이프를 일부러 페이지 스크롤에 양보하도록 설계돼 있어
(`touch-action:pan-y`, `applyTouchMode`) — 드래그가 `dragEnd` 없이 브라우저 스크롤에
가로채이는 게 오히려 정상 시나리오. 한 번이라도 이렇게 끊기면 `netBusy`가 영원히
`true`로 굳어 그 뒤로는 문서 선택/해제를 포함한 어떤 리사이즈도 캔버스에 반영 안 됨.

- [x] **수정**: `dragStart`도 `markBusy(2000)`로 통일(기존 `fit`/`moveTo`와 같은
      패턴). 드래그 중엔 `#netwrap` 크기 자체가 안 바뀌므로 타임아웃이 일찍 풀려도
      `relayout()`이 크기-불변 가드에 걸려 no-op — 안전.
- [x] **검증**: `pytest tests/test_graphview.py`(12건) 통과. (끊긴 터치 제스처를
      합성 이벤트로 완전히 재현하지는 못함 — hammer.js 내부 인식기가 단순
      `dispatchEvent(TouchEvent)`론 안 걸림. 메커니즘은 코드로 확정, 기존
      `fit`/`moveTo`에 이미 검증된 동일 패턴을 그대로 적용한 것이라 리스크 낮음.)
- [x] **배포**: `./deploy.sh` (CI 게이트 통과) 완료.

## 5. (논의) 모바일 UI를 그래프 축소 방식 대신 옆 슬라이드 패널로 개편하는 게 낫지 않나

사용자 제안: 문서 선택 시 그래프 세로 높이를 줄이는 현재 방식(#2에서 도입) 대신,
문서/설명을 **옆에서 슬라이드로 여닫는 패널**로 개편하면 어떤가.

**배경**: #2(그래프 접기)·#3(오버플로우)·#4(리사이즈 고착) 세 버그가 전부 "그래프
캔버스의 실제 렌더 크기와 CSS 박스 크기를 매번 동기화해야 한다"는 같은 취약점에서
파생됨 — vis-network 캔버스 리사이즈가 근본적으로 잘 깨지는 지점.
슬라이드/오버레이 패널로 가면 그래프 캔버스 크기 자체를 **절대 안 건드리므로** 이
버그 계열이 구조적으로 사라짐. 사용자 승인("해보자") 후 즉시 진행.

- [x] **구현**: 모바일 전용 개편(데스크톱 나란히 배치는 그대로 유지).
      - `#2`에서 도입한 `body.reading #netwrap{height:22vh}` 축소 규칙 **제거** —
        그래프는 이제 패널 상태와 무관하게 항상 고정 58vh.
      - `#panel`을 모바일에서 `position:fixed;inset:0` 화면 전체 슬라이드 오버레이로
        전환(`transform:translateX(100%)↔0`, `.25s` 트랜지션). 트리거는 기존
        `body.reading`(MutationObserver 기반 `syncReadingState()`) 그대로 재사용 —
        새 상태 관리 코드 추가 없이 CSS 타깃만 바꿈.
      - 고정 위치 `✕` 닫기 버튼(`#panelclose`, `body.reading` 일 때만 표시) 추가 +
        `closePanel()`(선택 해제 후 기본 힌트 복원 → MutationObserver 가 자동으로
        슬라이드 아웃).
- [x] **검증**: 로컬 mock 서버 + Playwright로 열기/닫기 전 과정에서 `#netwrap` 높이가
      489.5px(58vh)로 **항상 고정**됨을 실측 확인(예전엔 22vh로 변했음). 슬라이드
      열림(`translateX(0)`)·닫힘(닫기 버튼 `display:none`) 정상. 스크린샷으로 시각
      확인. `pytest tests/test_graphview.py`(12건) 통과.
- [x] **배포**: `./deploy.sh` (CI 게이트 통과) 완료.
## 6. #5 승인 후 모바일 목록 UX 2단계 개편 — 확장→리빌→리더, 뒤로가기 스택

사용자 지시: "슬라이드도 뒤로가기 동작하게 스택 쌓고. 아래 목록부 터치하면 글이 바로
선택되는게 아니라 목록이 그래프 위로 슬라이드해 올라오고. 이때 그래프 영역은
적당히 남겨서 거길 터치하면 글패널 다시 아래로 돌아가고. 목록부는 4대6비율로
즐찾과 글 패널이 배분하고. 목록이 슬라이드 업 된 이후에 글을 누르면 우측에서
크게보기 버튼이 밀어내기로 등장해서. 그걸 누르면 크게 보기창이 나오도록. 글
즐찾버튼은 지금처럼 놔두고."

**착수 전 확인(질문)**: 두 갈래 확정 후 진행.
- 문서 상세 패널(요약·노드·숨기기)은 → **모바일 한정으로 완전히 없애고 리더로 직행**
  (데스크톱은 기존 우측 패널 그대로 유지).
- 그래프 노드(원) 직접 탭 → **기존 전체화면 슬라이드 패널 그대로 유지**(이번 개편은
  "문서 목록" 흐름만 대상, 노드 상세는 안 건드림).

**구현(모바일 전용, 데스크톱 무영향)**:
- `#docs`를 `#wrap` 기준 `position:absolute;bottom:0`으로 전환 — `#netwrap`(그래프)은
  이제 이 흐름에서 **완전히 배제**(항상 고정 58vh, 절대 리사이즈 안 함 — #2/#3/#4
  버그의 공통 원인이었던 "캔버스 크기 동기화" 자체를 없앰). collapsed(기본) 22vh,
  `body.listopen` 46vh — 그래프는 항상 58vh 라 확장 시에도 위쪽 12vh 는 항상 탭
  가능(사용자가 말한 "적당히 남겨서").
- `#docs.haspinned #pinnedlist{flex:4 1 0}` / `#doclist{flex:6 1 0}` — 4:6 배분(즐찾
  없으면 목록이 전체 사용). `renderDocs()`에서 즐찾 유무로 `.haspinned` 클래스 토글.
  `#pinnedlist`의 예전 `max-height:32%`(#2 참고 — 원래도 모바일에서 안 먹던 값) 대신
  flex-basis 비율로 전면 교체.
  - `selectDoc(id)`를 모바일/데스크톱 분기: 모바일에서 목록이 접혀있으면 **첫 탭은
    확장만**(`pushUIState('list')` + `listopen` 클래스, 선택 안 함) — 펼쳐진 상태에서
    같은/다른 아이템 탭 시 그 아이템의 `revealedDocId` 만 세팅해 재렌더(선택 없음,
    로드도 없음). 데스크톱 분기는 기존 로직 그대로 보존.
  - 목록 아이템의 📖(크게읽기) 버튼에 `readbtn` 클래스 추가, 모바일 CSS로 기본
    `translateX(160%)`(밖으로 밀어둠) → `.docitem.revealed .readbtn{translateX(0)}`로
    슬라이드 인(밀어내기 애니메이션). ⭐(즐찾) 버튼은 그대로 항상 노출(요구사항).
  - `#netwrap` 클릭을 capture 단계에서 가로채 — `listopen` 중이면 `stopPropagation` +
    `closeTopUIState('list')`(그래프 탭 = 목록 접기), 아니면 평소대로 vis-network 가
    처리(노드 클릭 등 무관).
  - `#zoomctl`(+/− 버튼)이 `#netwrap` 바닥에 붙어있어 `#docs` 오버레이에 가려지던 문제
    — collapsed 높이(22vh) 위로 들어올림.
- **뒤로가기 스택**: `uiStack`(배열) + `pushUIState(name)`(열 때 `history.pushState`) +
  `closeTopUIState(name)`(in-app 컨트롤로 닫을 때 직접 안 닫고 `history.back()` 호출)
  + 전역 `popstate` 리스너(스택 pop 후 해당 UI 만 실제로 닫음: `collapseListUI()` /
  `closeReaderUI()`). `openReader()`가 `pushUIState('reader')` 하도록 연결, 기존
  `closeReader()`(✕·배경 탭·ESC 세 곳에서 호출)는 `closeTopUIState('reader')` 로 위임.
  목록(`list`)·리더(`reader`) 둘 다 이 스택 하나로 통일 — 리더가 목록 위에서 열리면
  뒤로가기 1번=리더만 닫힘, 2번=목록도 닫힘(중첩 정상 동작, 실측 확인).
- **범위 밖(의도적으로 안 건드림)**: 노드 상세 패널(#panel, `body.reading`)은 자체
  ✕ 버튼만 있고 이 뒤로가기 스택에는 안 엮음 — 반응형 MutationObserver 트리거와
  얽으면 상태 꼬임 리스크가 있어 이번 범위에서 제외(필요하면 별도 후속 작업).

- [x] **검증**: 로컬 mock 서버 + Playwright, 완전한 흐름 실측:
  - 접힌 목록 탭 → `listopen=true`, `#docs` 높이 389px(46vh), `history.length` +1.
  - 재탭 → `.docitem.revealed` 부여, 📖 버튼만 슬라이드 인(⭐는 그대로).
  - 📖 탭 → 리더 오픈 + `history.length` +1(총 +2).
  - 뒤로가기 1번 → 리더만 닫힘(목록은 열린 채 유지) — 스택 순서 정확.
  - 뒤로가기 2번 → 목록도 접힘(`#docs` 186.7px=22vh 로 복귀).
  - 그래프 영역(남은 위쪽) 탭 → 목록 접힘 + `history.length` 정상 복귀(그래프 탭도
    스택과 맞물려 동작).
  - **`#netwrap` 높이는 이 모든 과정에서 단 한 번도 안 변함**(489.5px=58vh 고정,
    핵심 설계 원칙 실측 확인).
  - 데스크톱(1400×900) 회귀 없음 — 문서 클릭 시 기존 우측 패널(요약 포함) 그대로 열림,
    `#panel` 360px·`#docs` 280px 폭 불변.
  - `pytest tests/test_graphview.py`(12건) 통과.
- [x] **배포**: `./deploy.sh` (CI 게이트 통과) 완료.

## 7. #6 배포 후 화면 하단에 빈 공간 생김

**사용자 증상**: "화면 하단에 빈공간이 생긴다." 스크린샷: 그래프+목록 아래로 큰 흰
여백이 있다가 브라우저 주소창이 나옴.

**원인(확정)**: #6에서 `#docs`를 `position:absolute`로 빼면서 `#wrap`의 실제 flow
높이가 `#netwrap` 하나(고정 `58vh`)로만 결정되게 됐다. 그런데 헤더(`#bar`+
`#legendbar`, 특히 관계 타입 칩이 많아 여러 줄로 줄바꿈되는 `#legendbar`)의 실제
높이는 기기·데이터양마다 다르고, `58vh`는 그 변동을 전혀 고려하지 않은 고정값이라
"헤더 실측 높이 + 58vh"가 실제 화면 높이보다 짧아지면 그 차액만큼 화면 맨 아래가
빈다(#6 로컬 검증 때는 mock 데이터가 적어 헤더가 짧아 안 드러남 — 실사이트는 관계
타입이 많아 헤더가 훨씬 길어서 노출됨).

- [x] **수정**: `body{display:flex;flex-direction:column}`(모바일) 로 바꾸고
      `#wrap{flex:1;min-height:0}`, `#netwrap{flex:1;min-height:260px}`(고정 `58vh`
      제거)로 — 헤더가 몇 줄이든 그래프가 항상 "화면 - 헤더 실측 높이"를 정확히
      채운다. `#docs`/`#zoomctl`의 높이 기준도 `vh`(뷰포트 절대값) 대신 `%`(이제
      `#netwrap`과 정확히 같은 높이인 `#wrap` 기준)로 바꿔 collapsed 38%·listopen
      79%가 헤더 길이와 무관하게 항상 같은 비율을 유지하게 함.
- [x] **검증**: 로컬 mock 서버 + Playwright — `document.documentElement.scrollHeight
      === window.innerHeight`(844=844, 빈 공간·불필요한 스크롤 둘 다 없음) 확인,
      collapsed/listopen 각각 `#docs` 높이가 `#netwrap`의 정확히 38%/79%로 계산됨
      실측(277px/575px, `netwrapH=727px` 기준). `pytest`(12건) 통과.
- [x] **배포**: `./deploy.sh` (CI 게이트 통과) 완료.

## 8. #6/#7 모바일 목록 UX 후속 피드백 4건

사용자 피드백 원문(2026-07-15):
1. "목록이 슬라이드 되기 전엔 터치로 개별이 스크롤되면 안되고 슬라이드를 시켜야지."
2. "슬라이드 후 문서 개별 선택 시 그래프가 노드로 이동하는 기존기능이 누락됨."
3. "문서 선택 시 우측 슬라이드가 찔끔 나와있고. 터치나 당기면 노드설명이 적힌 패널이
   나오는 식으로."
4. "목록에 크게읽기 버튼은 폰에선 없애기로 했었지? 대신 더 큰 버튼이 각 글 아이템
   우측에서 나타나고 누르면 크게보기가 나오는 식."

- [x] **1. 목록 collapsed 상태에서 터치 스크롤 금지**: `body:not(.listopen)
      #pinnedlist, body:not(.listopen) #doclist{overflow:hidden}`(모바일)로 collapsed
      동안 두 리스트의 자체 스크롤을 꺼서 스와이프도 확장 트리거만 타게 함. 펼친
      뒤(`body.listopen`)엔 base 의 `overflow-y:auto` 그대로 복귀(모바일 규칙에
      `body.listopen` 오버라이드가 따로 없어 자동으로 원복).
- [x] **2. 문서 선택(reveal) 시 그래프 노드 이동 복원**: `fitToDocNodes(id)` 로
      데스크톱·모바일 공유 함수로 뽑아내 데스크톱 `selectDoc()`과 모바일 리빌
      분기 양쪽에서 호출(모듈화 — 로직 중복 없이 공유).
- [x] **3. 문서 선택 시 우측에 상세 패널 "찔끔 peek" — 당기면/탭하면 열림**: 새 상태
      `body.peek` 도입 — 문서 리빌 시 `#panel`을 `translateX(calc(100% - 26px))`로
      살짝 걸쳐 보이게(내용은 아직 안 불러옴, `defaultHint()` 그대로라 안 보여도
      무해). 명시적 손잡이 `#panelpeek`(◂, peek 중에만 노출) 탭 또는 `#panel`
      자체를 탭하면 `openPeekPanel()` → `loadDocPanel()`로 내용을 그때 불러오고
      `body.reading`으로 전환해 기존 전체 슬라이드 애니메이션 재사용. ✕ 닫기는
      `closePanelOrPeek()`로 — 문서가 여전히 선택된 채면 완전히 안 닫고 다시
      peek 로(재탭하면 재오픈), 노드 클릭발이거나 데스크톱이면 기존처럼 완전히 닫음.
      **버그 발견 및 수정**: `loadDocPanel()`의 stale-응답 가드가 `activeDoc`만
      체크해서 모바일에선 `activeDoc`이 항상 `null`이라 fetch 응답이 매번 버려져
      "문서 불러오는 중…"에 영원히 멈춤 — `revealedDocId`도 같이 체크하도록 수정.
- [x] **4. 목록 아이템의 크게읽기 버튼 — 리빌 시 훨씬 크게**: `readbtn`을 `.docactions`
      밖의 형제 요소로 분리(그 안에 있으면 `.docactions`의 `position:absolute`가
      포함 블록이 되어 아이템 전체 높이를 차지하는 절대배치가 안 먹힘) — 데스크톱은
      base CSS 로 원래 자리(즐찾 버튼 왼쪽, 작은 아이콘)에 위치 고정, 모바일에서만
      아이템 우측 전체 높이를 차지하는 큰 버튼(스와이프 액션 스타일, `top:26px`로
      ⭐ 즐찾 버튼 자리와 안 겹치게)으로 리빌.
- [x] **검증**: 로컬 mock 서버 + Playwright, 4건 전부 실측 —
      collapsed/listopen 시 pinnedlist·doclist overflow hidden↔auto 전환,
      리빌 시 `body.peek=true`+`#panelpeek` 노출+아이템에 큰 파란 버튼(데스크톱은
      기존 작은 아이콘 그대로, `position:absolute;top:6px;right:7px`), peek 탭 →
      전체 패널 정상 오픈(요약·이 문서의 노드·숨기기·크게읽기 전부 로드 확인,
      loadDocPanel 가드 버그 잡은 뒤 재검증). 콘솔 에러 없음. `pytest`(12건) 통과.
- [x] **배포**: `./deploy.sh` (CI 게이트 통과) 완료.

## 9. #8 배포 후 추가 피드백 5건 — 구현 완료(이번 세션)

**사용자가 "같은 걸 반복해서 시키고 있는 기분"이라고 지적** — 아래 근본 원인 항목
(★표시) 꼭 먼저 읽을 것. 이번 세션에서는 원인만 진단해두고 **구현은 안 함** —
사용자 요청("정리하고 핸드오프하고 새 대화에서 계속하자")에 따라 다음 세션이 여기서
바로 이어받아 진행.

**사용자 피드백 원문(2026-07-15)**:
1. "크게읽기 버튼에 하이라이트가 안예쁘고."
2. "글 선택시 화살표버튼이 나오는것보단, 실제 나와야하는 창이 미리 빼꼼 나와있는게
   났다. 내가 끌어서 꺼낼수 있으니."
3. "목록이 접혀있을때 내가 드래그하면 거기 맞춰 목록이 위로 슬라이드해 올라와야
   좋겠다."
4. "글선택 후 그래프 영역 만지면 엉뚱한 위치로 바로 이동해버리는 문제가 또 있다."
5. "글 선택시 데탑버전엔 노드 색도 바꾸고 여러가지 있는데 폰은 또 안되네?"

### ★ 근본 원인(공통) — 왜 자꾸 같은 종류의 구멍이 남는가

#8까지 개편하면서 모바일 전용 상태(`revealedDocId`)를 데스크톱의 기존 상태
(`activeDoc`)와 **별도 병렬로** 도입했다. 문제는 `activeDoc`에 의존하는 기존 로직이
코드베이스 곳곳에 있는데, 모바일 흐름은 `revealedDocId`만 세팅하고 `activeDoc`은
안 건드려서 그런 로직들이 모바일에서 전부 조용히 무동작이었다:
- `loadDocPanel()`의 stale-응답 가드(#8에서 발견·수정 완료 — `activeDoc`만 보다가
  모바일에서 응답이 영원히 버려지던 버그).
- **`applyView()`가 바로 이 패턴의 또 다른 사례(#5 원인, 아래 확정)** — 아마 다른
  곳에도 더 있을 수 있음, 전수 조사 필요.

**권장 방향**: `revealedDocId`를 유지하되(리빌 UI 전용 개념이라 필요), 모바일에서
문서를 리빌할 때 **`activeDoc`도 같이 세팅**(`selectDoc()`의 모바일 분기,
`revealedDocId===id`가 아닌 else 브랜치)하고 `applyView()`를 호출하게 하는 게
제일 간단 — `activeDoc`을 참조하는 기존 로직(아래 5번 항목 참고)이 전부 "공짜로"
모바일에서도 동작하게 된다. `grep -n "activeDoc" src/claire/graphview.py`로 전체
사용처를 먼저 훑고, 모바일에서 같이 세팅해도 문제없는지(부작용 없는지) 하나씩
확인 후 진행할 것 — 코드 읽어보니 현재 사용처(노드 상세의 "← 문서로" 백링크,
"더 알아보기" 조사의 doc_id 컨텍스트, `docItemHtml`의 `.active` 클래스) 전부
모바일에서도 켜져도 무해해 보임(오히려 지금 빠져있던 기능들임).

---

- [x] **5. (★근본원인 직결, 가장 쉬움) 문서 선택 시 노드 색 하이라이트 안 됨**:
      **원인 확정**(`applyView()`, `graphview.py` — `grep -n "function applyView"`):
      `const hasFilter = activeDoc || highlightSet || pathActive;` 와
      `if(activeDoc) match = match && (n.sources||[]).includes(activeDoc);` 전부
      `activeDoc`만 봄. 데스크톱 `selectDoc()`은 `activeDoc` 세팅 후 `applyView()`를
      호출하는데, 모바일 리빌 분기(`selectDoc()` 안, `mobileMQ.matches` 블록)는
      `revealedDocId`만 세팅하고 `activeDoc`/`applyView()` 둘 다 호출 안 함 — 그래서
      모바일에서 노드 색·테두리 강조(dim/lit)가 전혀 안 먹힘.
      **수정 방향**: 위 "근본 원인" 권장대로 모바일 리빌 시 `activeDoc = id`(해제 시
      `null`)도 같이 세팅 + `applyView()` 호출. `revealedDocId`는 리빌 UI(📖 버튼
      노출·peek 트리거) 전용으로 계속 씀 — 둘이 같은 값이어도 역할은 분리 유지.
- [x] **4. 문서 선택 후 그래프 영역 터치 시 엉뚱한 위치로 이동**:
      **유력 원인(코드 확정, 실기기 재현은 아직 안 함)**: `#netwrap`의 "탭하면 목록
      접기" 핸들러(`graphview.py`, `document.getElementById('netwrap')
      .addEventListener('click', ...)` 근처, capture 단계)가 **`'click'` 이벤트만
      가로챈다**. 브라우저는 포인터가 down→up 사이 일정 거리 이상 움직이면 `click`
      을 안 쏘고 드래그로 처리 — 즉 이 핸들러는 **탭에는 반응하지만 드래그(팬
      제스처)는 그대로 통과시켜 vis-network(hammer.js)의 그래프 팬 핸들링으로
      새버린다.** 문서 리빌 직후엔 `fitToDocNodes()`가 `net.fit({animation:true})`
      를 애니메이션으로 실행 중인데(카메라가 아직 보간 중), 이 타이밍에 사용자가
      바로 손을 대고 드래그하면 vis 내부적으로 "애니메이션 도중 사용자 드래그
      시작"이 겹쳐 카메라가 튀는 것으로 추정(비슷한 클래스의 문제를 #4(TODO 상단)
      에서 이미 한 번 다룸 — `netBusy`/`dragStart` 관련, 그때는 리사이즈 문제였고
      이번은 카메라 위치 문제라는 차이).
      **수정 방향(둘 다 권장, 함께 적용)**:
      (a) 리스트가 열려있는(`listopen`) 동안엔 애초에 그 "적당히 남은 그래프 스트립"
      에서 pan/드래그 자체를 못 하게 막는 게 제품적으로도 맞다(좁은 스트립에서
      의미 있는 팬은 어차피 안 됨) — `body.listopen #net, body.listopen #net canvas`
      에 `touch-action:none` 이 아니라 애초에 vis 의 hammer 제스처 자체를
      비활성화하거나, `pointerdown`(또는 `touchstart`) 을 capture 단계에서 막아
      hammer 로 전달되기 전에 `closeTopUIState('list')` 로 흡수(지금의 `click`
      대신 `pointerdown`/`touchstart` 로 바꾸면 드래그 시작 시점에 바로 잡아서
      hammer 가 아예 못 받게 할 수 있음).
      (b) `fitToDocNodes()` 호출 시점을 재검토 — 애니메이션 도중 사용자 입력이
      들어오면 vis 쪽에서 애니메이션을 깨끗이 취소하고 사용자 드래그로 넘어가는지
      실기기(또는 CPU 스로틀링)로 재현해 확인. 안 되면 `net.fit()` 애니메이션
      진행 중엔 그래프 영역 터치를 아예 무시(스와이프 무시)하는 방어도 고려.
      **실기기 재현이 안 된 상태(로컬 Chromium 으로는 이 클래스의 터치 타이밍
      버그를 못 잡을 가능성 높음 — #3 때 GPU 글리치 오진 사례 참고)** — 다음
      세션은 먼저 재현 시도(CPU 스로틀링 + 실제 touch 이벤트 시퀀스)부터.
- [x] **2+3. 탭-토글 대신 실제 드래그-추종 제스처로 교체(패널 peek·목록 확장 둘 다)**:
      사용자 요구 핵심: 지금은 CSS 트랜지션 + 탭/버튼으로 스냅 열림/닫힘만 되는데,
      **손가락을 따라 실시간으로 따라오다가 놓으면 스냅**되는 진짜 드래그 제스처를
      원함(표준 바텀시트/사이드드로어 패턴). 현재 상태:
      - 패널(#panel) peek: `body.peek #panel{transform:translateX(calc(100% -
        26px))}` 로 실제 패널 자체가 이미 살짝 걸쳐 보이긴 함(사용자 요구사항
        2번의 "실제 나와야하는 창이 미리 빼꼼"은 이미 구현돼 있음) — 문제는 그
        위에 얹은 별도 손잡이 버튼(`#panelpeek`, ◂)이 훨씬 시각적으로 두드러져서
        사용자가 "화살표 버튼만 보인다"고 느낀 것으로 추정. peek 상태에서 실제
        패널 내용이 `defaultHint()`(안 불러온 상태)라 걸친 26px 조각이 텍스트도
        없이 밋밋해서 존재감이 없는 것도 원인.
      - 목록(#docs) 확장: 지금은 아이템 탭(`selectDoc`) 이 유일한 트리거, 드래그
        추종 전혀 없음(collapsed 시 overflow:hidden 으로 스크롤만 막아둔 상태,
        #8-1 참고).
      **구현 방향(다음 세션)**: 둘 다 같은 패턴 — `touchstart`(또는 `pointerdown`)
      로 시작점 기록 → `touchmove`로 델타 계산해 해당 요소의 `transform`(또는
      height)을 인라인 스타일로 실시간 갱신(트랜지션은 이 구간에서 꺼야 안 끊기고
      부드러움) → `touchend`에서 이동 거리/속도로 열기·닫기 스냅 판정, 트랜지션
      다시 켜고 최종 CSS 클래스(`listopen`/`peek`/`reading`)로 정착. 표준 참고
      구현 패턴이 많음(바텀시트 라이브러리들의 공통 로직) — 처음부터 velocity
      계산까지 욕심내지 말고 "이동 거리 50% 넘으면 열림, 아니면 원위치" 정도로
      단순하게 시작해도 충분할 것.
      **peek 손잡이(#panelpeek) 처리**: 위 드래그 구현 후에는 손잡이 버튼을
      없애거나(패널 조각 자체가 드래그 핸들 겸 탭 타겟) 훨씬 작고 수수하게 —
      사용자가 "실제 패널이 미리 보이는 느낌"을 원하므로 화살표 버튼이 시선을
      뺏지 않게.
- [x] **1. 크게읽기(📖) 리빌 버튼 시각 디자인 개선**: 사용자 확인 결과 "그냥 텍스트로,
      예쁘게" — 아이콘 전용 각진 풀하이트 스트립 대신, 텍스트 라벨("크게읽기")만 보이는
      둥근 모서리(`border-radius:10px`) + 그림자(`box-shadow`) 배지로 교체, 아이템 가장자리에
      여백을 둬 떠 있는 느낌으로. 데스크톱은 기존 아이콘 전용 자리 그대로(마우스는 정밀해서
      문제없었음, `.rblabel{display:none}` 로 데스크톱만 숨김).

**구현(이번 세션, `src/claire/graphview.py`)**:
- **근본원인 통합(★)**: `selectDoc()` 모바일 분기에서 리빌 시 `revealedDocId` 뿐 아니라
  `activeDoc` 도 같이 세팅(해제 시 둘 다 `null`) + `applyView()` 호출. `collapseListUI()`
  (뒤로가기/그래프탭으로 목록 접힘)에도 동일하게 `activeDoc=null; applyView()` 추가.
  → 5번(노드 하이라이트)이 이 통합만으로 자동 해결됨(별도 로직 불필요, 예상대로).
- **4번**: `#netwrap` 의 "탭하면 목록 접기" 핸들러를 `'click'`(탭에만 반응, 드래그는
  hammer 로 새어나감) 에서 `'pointerdown'`(capture, `stopPropagation`+`preventDefault`)
  으로 교체 — 드래그 시작 시점에 바로 흡수해 vis(hammer)가 이벤트 자체를 못 받는다.
  추가로 `net.fit()`/`net.moveTo(animation)` 진행 중(700ms) `dragView` 를 잠시 꺼서
  카메라 애니메이션과 사용자 드래그가 겹치는 것 자체를 방지(실기기 재현은 여전히
  못 했으나, 코드 레벨 경합 조건은 확정적으로 제거됨 — TODO 권장 방어책).
- **2+3번**: 공유 헬퍼 `attachDragSheet(el,{axis,enabled,getMetrics,onMove,onEnd})` 신설
  — pointerdown/move/up 으로 실시간 추종(드래그 중 트랜지션 off) 후 이동 거리 50%
  기준 스냅(TODO 권장 단순 휴리스틱), 드래그 직후 1회 click 을 흡수해 탭 오인 방지.
  `#docs`(collapsed 상태에서만, 세로, 높이 %) 와 `#panel`(peek/reading 상태, 가로,
  translateX)에 각각 적용 — 목록은 위로 끌면 펼쳐지고, 패널은 peek 조각을 왼쪽으로
  끌면 열리고 반대로 끌면 다시 peek 로.
- **1번**: 위 설명대로 텍스트 라벨 + 둥근 모서리 + 그림자로 교체.
- **핸드오프 이후 사용자 추가 피드백(세션 중)**: "즐찾버튼이랑 손가락 두꺼우면
  잘못누르겠다 — 좌측으로 밀까?" → ⭐(즐찾) 버튼을 모바일 전용으로 아이템 좌측으로
  이동(`docs{padding-left:32px}` + `.docactions{left:6px}`), 우측은 리빌 배지 전용
  공간으로 완전히 분리(데스크톱은 원래 우측 자리 그대로 유지).

**검증**: 로컬 mock 서버(문서 8개, 즐찾 3개, 엔티티 4개) + Playwright(모바일 390×844) —
①문서 리빌 시 activeDoc 소속 노드만 opacity 1/borderWidth 3, 해제 시 전부 원복 실측.
②listopen 중 그래프 스트립에 pointerdown+move+up(드래그) 시퀀스를 흘려도 카메라
좌표 불변 + 목록만 정상 접힘(popstate 경유) 확인. ③`#docs` 드래그: 8px 미만은
무반응(탭 취급), 50% 미만 이동은 원위치 스냅, 50% 초과는 `listopen`+히스토리 push
정상. ④`#panel` 드래그: peek→(왼쪽으로 끌기)→reading(문서 내용 로드 확인)과
reading→(오른쪽으로 끌기)→peek 양방향 정상. ⑤데스크톱(1400×900)에서 동일 드래그
시퀀스 발사 시 `mobileMQ` 가드로 완전 무반응(회귀 없음), activeDoc 하이라이트·패널
폭 등 기존 동작 그대로. `pytest tests/test_graphview.py`(12건) + `scripts/ci.sh`
전체(205건) 통과.
**배포**: 아직 안 함 — 사용자 확인 후 `./deploy.sh` 진행 예정.

