# TODO 2026-08-15

## 1. MCP 지원 추가 (조사+계획, 미구현)

**요청**: 사이트(claire_bible 지식베이스)에 MCP 지원을 추가 — 축적된 지식을 읽고
쓰는(당장은 읽는) 경로를 다양화. 기존 owner/readonly 토큰 권한 체계를 MCP에도
동일 적용: **토큰 없으면 MCP 존재·사이트 존재 자체를 모르게**(404), 토큰 있으면
권한에 맞는 툴만 노출.

**사용자 확정 사항**(대화 중 정정):
- v1 스코프는 **read 전용**(ingest/synthesize/dedup 등 쓰기 툴은 나중).
- MCP 클라이언트는 Claude Code/Desktop 한정이 아니라 **다른 하네스(hermes 등)에서도**
  씀 → "최신 표준에 가까운" 구현 필요(hand-roll보다 공식 SDK 선호).
- 기존에 이미 원격에서 HTTP로 서비스 중인 인프라(같은 도메인·토큰·DB)를
  **재사용**하는 방향 선호.

**조사 중 발견(별도 이슈, 이번 세션에서 코드/설정 변경 없음)**:
- [ ] `docs/EXTERNAL_ACCESS.md`가 "외부노출 미실행/설계만"이라 되어있으나 실제로는
      `claire.blackan.net`이 nginx+Cloudflare로 이미 라이브. nginx 주석은
      "인증=Cloudflare Access"라 하지만 외부 curl 검증 결과 **CF Access가 실제로
      걸려있지 않음**(앱 레벨 404가 그대로 통과 — 보안헤더까지 앱 것과 일치).
      앱 토큰 게이트가 사실상 유일한 방어선. 문서 갱신 + CF Access 실제 설정 여부는
      **사용자 확인 필요**(다음 세션 또는 별도 논의).
- [x] 원격 `.env`에 `CLAIRE_READONLY_TOKEN` 미설정 확인함 — **설정 불필요로
      결론**(사용자 결정: "지금의 기한제 토큰이면 충분해, 함부로 격리레벨을
      낮추려하지 마"). MCP 인증은 정적 토큰이 아니라 기존 `/web`·`/webro`
      텔레그램 동적 세션(`X-Session` 헤더)을 그대로 재사용하기로 확정.

**계획**: `docs/MCP_SUPPORT.md` 작성 완료(설계만, `EXTERNAL_ACCESS.md`/
`SYNTHESIS_REDESIGN.md`와 동일한 "설계 문서 먼저" 관례). 코드 구현은 사용자 승인
후 별도 세션.

## 2. MCP 지원 구현 (M1) — 같은 세션에서 이어서 진행, 구현 완료

브랜치 `feature/mcp-support`. 구현 내용:
- `src/claire/api/mcp_tools.py`(신규) — read 전용 10툴(resolve_entity/search/
  neighbors/path/context/overview/node/documents/document/stats). 각 툴은
  `_impl(conn, ...)` 순수 함수 + `@mcp.tool()` 얇은 래퍼로 분리(테스트 용이).
- `src/claire/api/server.py` — `/mcp` POST 라우트 + aiohttp↔ASGI 어댑터(scope/
  receive/send 수동 구현) + lifespan 훅 + `READONLY_PATHS`에 `/mcp` 추가.
  `run_api()`를 `build_app(s)`(구성만) + `run_api()`(실행)로 분리(테스트용).
- `src/claire/graphview.py` — `synthesis_context`에 `compact` 옵션 추가(기존
  호출부는 기본값 False라 하위호환 100%).
- `pyproject.toml`/`uv.lock` — `mcp==2.0.0` 의존성 추가(`uv add mcp`).
- 테스트: `tests/test_mcp_tools.py`(18개, 순수 함수), `tests/test_api.py`(7개,
  아이오홉프 TestClient로 게이트/세션 경계 — 무토큰 404, 무효세션 404, 세션
  재발급 시 이전 토큰 무효화, 알수없는 툴 이름 처리 등). **전체 294 passed**,
  `scripts/ci.sh` 게이트 통과.

**실제 검증(코드 리뷰·문서화만이 아니라 직접 돌려서 확인)**:
- 합성 그래프(6엔티티/5관계/1문서, scratchpad DB)로 로컬 `serve-api` 기동 →
  탐색 루프 전체(resolve_entity→neighbors→search near_ids→path→overview→
  context→document) curl로 실행해 설계한 흐름이 실제로 동작함을 확인.
  무토큰 404, 무효세션 404, 세션 재발급 시 구세션 즉시 무효화(§5 트레이드오프)
  도 실측.
- 공식 `mcp` SDK의 **진짜 클라이언트**(`ClientSession`+`streamable_http_client`,
  내가 짠 게 아니라 SDK 자체 핸드셰이크)로 재접속해 initialize→list_tools→
  call_tool 왕복 확인 — `stateless_http=True`로 `Mcp-Session-Id` 없이도
  정상 동작(hermes 등 제3자 하네스 호환성 관련 advisor 우려 해소).
- advisor 리뷰에서 잡힌 버그: `search` 결과가 말없이 잘릴 수 있었고
  (`truncated`/`omitted` 추가로 수정), `entity_type`/`near_ids` 필터를 걸 때
  FTS 후보 풀이 좁으면 실제 매치를 거짓음성(0건)으로 보고할 수 있었음
  (필터 사용 시 headroom 200으로 확대, 회귀 테스트로 고정).
- 원격 `.env`의 `CLAIRE_PUBLIC_URL=https://claire.blackan.net`이 nginx
  `server_name`과 정확히 일치함을 확인(배포 후 `allowed_hosts` 421 위험 없음).

**미해결/다음 결정**: docs/MCP_SUPPORT.md §12 잔여 결정사항(EXTERNAL_ACCESS.md
정정 여부, Cloudflare Access 실제 사용 여부, 슬라이딩 TTL 대응 여부) +
**master 병합 여부**(배포는 브랜치 그대로가 아니라 병합 후로 할지 사용자
확인 필요, `deploy.sh`는 working tree를 그대로 rsync하므로).

## 3. MCP M1 배포 후 실사용 마찰 — `documents`/`node` 개선

M1을 master 병합·배포 후, 실제 MCP 클라이언트(Claude Code 자신)로 붙어
탐색 루프를 써보고 최신 소식 기사 2편을 작성해보는 과정에서 겪은 마찰:

**발견된 문제**:
- `documents(limit=300)` 호출이 134KB/3,100줄로 도구 응답 한도를 넘겨 파일
  강제저장 — 두 번 발생(그래프 탐색 중 한 번, PDF용 fetched_at을 찾으려고
  전체 덤프를 grep으로 우회하려던 중 또 한 번).
- `document`/`node` 응답에 문서 수집 시각(`fetched_at`)이 없어 정확한 날짜를
  알아낼 방법이 grep 우회뿐이었음.
- advisor 재검토에서 지적: `node()`는 `documents`보다 **더 심각한** 동일 결함
  — 소스 문서마다 본문 전문(`detail`)을 무제한으로 포함해, 실측으로 AGENTS.md
  엔티티(소스 2개) 조회 시 기사 2편 전문이 그대로 반환됨. `overview()`의
  `Claude Code` 허브는 `source_count: 48` — 그 노드에 `node()`를 부르면 응답이
  터질 게 확실.

**수정 내용**:
- `documents` 툴: `MAX_DOCUMENTS=100` 하드캡(그 이상 요청해도 잘림) +
  `since`(날짜 또는 전체 ISO8601)/`query`(제목·URL 부분일치) 필터 추가 +
  `truncated`/`omitted` 플래그. `db.documents_timeline`/`documents_count`에
  선택적 파라미터로 구현(웹 UI 호출부는 무필터 그대로라 하위호환 유지).
  잘못된 `since` 문자열은 예외 대신 `{"error": ..., "got": ...}` 반환(advisor 지적).
- `node` 툴: 소스 문서 최신 10개(`MAX_NODE_DOCUMENTS`)까지만, 기본은 본문
  전문(`detail`) 대신 요약(`summary`)만 포함, `full=True`로 opt-in 시 그 10개에
  한해 전문 포함. `documents_truncated`/`documents_omitted` 플래그(advisor
  블로킹 지적으로 추가 — documents보다 먼저 고쳤어야 할 더 큰 구멍이었음).
- `document`/`node`/`documents` 세 툴 모두 `fetched_at`을 원시 epoch가 아니라
  **ISO8601, UTC 타임존 명시**(`+00:00`)로 노출 — 서버가 호출자의 로컬
  시간대를 가정하지 않는다는 원칙(docs/MCP_SUPPORT.md §11.2에 근거 기록).
- 테스트 9개(타임존 명시, since/query 필터, 하드캡, truncated/omitted, 잘못된
  since 입력) + node 관련 3개(summary-only 기본값, full=True opt-in, 캡+플래그)
  추가. `scripts/ci.sh` **307 passed**로 통과.
- `docs/MCP_SUPPORT.md` §6/§11.2 실측 정정(node/documents "그대로 재사용
  가능" → 후처리 필요로 갱신, fetched_at UTC 결정 근거 기록).

**상태**: 코드/테스트/문서 완료, CI 게이트 통과, master 커밋(`a776540`)·push·원격 배포
완료(6개 컨테이너 정상 기동 확인).

## 4. 기사 PDF 산출물

개선된 `document` MCP 툴(fetched_at ISO8601 UTC)로 두 기사의 출처 15개 전부 정확한
수집일자를 재확보 — 사전 grep 우회 없이 라이브 MCP 호출만으로 완료(이번 개선의
직접적 효과 확인). KST(UTC+9) 환산값과 원본 UTC ISO8601을 병기해 A4 4페이지
PDF로 제작(`google-chrome --headless --print-to-pdf`, poppler 미설치라 pymupdf로
페이지 렌더링해 육안 검증). 사용자에게 전달 완료.
