# TODO 2026-07-24 — 모바일 그래프뷰 UX 버그 5건 + 깃 시크릿 점검

사용자가 구두로 보고한 모바일 UX 버그 5건. 작업 협약(CLAUDE.md)에 따라 먼저
항목별로 정리한 뒤 하나씩 처리한다. 코드 조사로 원인까지 확정한 항목은 원인도
같이 적었다(추정은 "추정"이라 명시).

## 0. 깃에 시크릿 커밋된 게 있는지 점검 (사용자 요청)

- [x] **점검 완료, 문제 없음 확인**:
  - `.env`가 `.gitignore`에 있고, `git log --all --full-history -- .env` /
    `git log --all --diff-filter=A --name-only` 전체 스캔 결과 `.env`(또는
    `.env.*`) 파일이 히스토리 어느 커밋에도 추가된 적 없음.
  - `git log --all -p`에서 `api_key=`, `token=`, `secret=`, `password=` 등
    패턴으로 실제 값이 박힌 라인 검색 → 매치 없음.
  - 현재 추적 파일 전체에서 `BOT_TOKEN`/`API_KEY`/`SECRET_KEY` 등을 그렙 →
    유일한 매치는 `src/claire/extract/gemini_provider.py`의
    `genai.Client(api_key=settings.gemini_api_key)`(환경변수 참조, 하드코딩
    아님).
  - `.env.example`은 값이 전부 빈 템플릿.
  - **결론**: 현재까지 커밋 이력에서 시크릿 유출 흔적 없음. 추가 조치 불필요.

## 1. 폰에서 목록 펼치기 + 우측 패널(peek/reading) 펼친 상태에서 뒤로가기 → 둘 다 한번에 닫힘

**증상(원문)**: "폰에서 목록+우측패널 펼치기해서 뒤로가기하니 둘다 한번에닫힘"

- [x] **원인 확정**: `selectDoc()`의 모바일 분기(`graphview.py:1743-1771`)가
      `listopen` 상태에서 문서를 탭하면 `body.peek`(또는 `reading`) 클래스만
      직접 추가하고 `pushUIState()`를 호출하지 않는다. 즉 뒤로가기 스택
      (`uiStack`)에는 `'list'` 한 건만 쌓여있는 상태. 뒤로가기 1번 →
      `popstate` → `uiStack.pop()`이 `'list'`를 꺼내 `collapseListUI()` 호출 →
      이 함수가 `classList.remove('listopen','peek','reading')`를 **한
      번에** 실행(`graphview.py:704-705`) → 목록과 우측 패널이 동시에 닫힘.
      우측 패널 오픈이 애초에 별도 히스토리 항목이 아니었던 게 근본 원인.
- [x] **수정**: `openMobilePanelUI()` 헬퍼 추가 — 패널이 닫혀있을 때만
      `pushUIState('panel')` 호출(`selectDoc` 오픈 분기, `loadNode`, `openPeekPanel`
      idle 오픈 경로 전부에서 호출). `popstate`에 `'panel'` 케이스 추가 →
      `closePanel()`로 실제 정리. `closePanelOrPeek()`/재탭-닫기도
      `closeTopUIState('panel')`을 거치도록 통일(직접 클래스 조작 제거, 히스토리와
      항상 일치). 부작용 방지: `#netwrap` pointerdown(그래프 영역 탭→목록 접기)이
      `closeTopUIState('list')` 하드코딩이라 패널이 스택 위에 있으면 no-op 되던 걸
      "스택 맨 위 아무거나 닫기"로 일반화.
- [x] **검증(로컬 mock `serve-api` + Playwright, 실제 브라우저 뒤로가기)**: 목록
      펼치기(JS `selectDoc`)→문서 탭(peek)→**실제** `page.go_back()` 1번 →
      `listopen` 유지 + `peek`/`reading`만 해제 확인(PASS). 뒤로가기 2번째→
      `listopen`도 해제 확인(PASS).

## 2. 우측 패널에서 노드 터치 → 요약 팝업이 뜨는데 닫을 방법이 없음 (UX도 안 맞음)

**증상(원문)**: "우측패널에 노드 터치하면 팝업이 뜨는데, 이걸 닫을방법이 없음.
ux도 안맞음. 그나마 괜찮으려면, 우측 패널 펼친 상태로, 노드를 터치하면, 화면
아래에서 팝업이 올라와서 보여지고, 다른곳 터치하면 팝업이 닫히는 것."

- [x] **원인 확정**: 우측 패널의 "이 문서의 노드" 버튼(`.nodebtn`,
      `graphview.py:1849-1850`)이 `onmouseenter="peekNode(...)"` /
      `onmouseleave="leaveNode()"`로 짜여있다 — 데스크톱 hover 팝업
      (`showNodePop`)을 재사용한 것. 모바일 터치는 일부 브라우저에서 탭 시
      `mouseenter`는 합성 발화되지만 다른 곳을 만져도 `mouseleave`가 절대 안
      온다(포인터가 실제로 그 엘리먼트를 벗어나는 이벤트가 없음) → 1.5초 뒤
      뜬 `#nodepop` 팝업이 영원히 안 닫힘. 그래프 캔버스 위 노드
      hover(`net.on('hoverNode'/'blurNode')`)도 같은 구조라 동일 버그 있을 수
      있음(모바일에서 캔버스 노드 탭 시 vis 가 hoverNode 를 쏘는지는 미확인).
- [x] **사용자 결정**: 두 방향(①모바일에서 팝업 자체 제거 vs ②하단 시트로 재구현)
      중 ①(제거) 선택 — 탭 한 번으로 `focusNode()`→`loadNode()`가 이미 그래프
      이동+전체 노드 상세(우측 패널)를 보여주므로 같은 정보의 팝업은 중복.
- [x] **수정**: `peekNode()`(우측 패널 `.nodebtn` hover)와 `net.on('hoverNode')`
      (캔버스 노드 hover, 동일 구조라 같은 버그 소지) 양쪽에 `if(mobileMQ.matches)
      return;` 추가 — 데스크톱 전용으로 한정. `leaveNode()`/`blurNode`는 그대로
      둬도 무해(호출돼도 이미 안 뜬 팝업을 숨기려 할 뿐).
- [x] **검증**: 코드 리뷰로 확인(모바일 분기에서 `showNodePop` 호출 경로 자체가
      아예 진입 안 됨 — 진입 안 하니 안 닫히는 문제도 구조적으로 발생 불가).
      `pytest tests/` 214건+서브테스트6 전체 통과(관련 회귀 없음).

## 3. 우측 패널이 안 나온 상태에서 끌어서 열 수 있는 손잡이(화살표)가 없음

**증상(원문)**: "우측 패널이 나온상태에서 우측으로 슬라이드하면 화면이
닫히는게 잘 됨. 근데 그런 ux라면 패널이 안나온 상태에서 패널의 파란 화살표를
잡아서 왼쪽으로 끌면 슬라이드로 나와주면 좋겠음."

- [x] **원인 확정**: 우측 패널을 손잡이로 당겨 열고 닫는 기능 자체는 이미
      있다(`attachDragSheet(#panel, {axis:'x', ...})`,
      `graphview.py:1061-1083`) — 다만 `enabled: () =>
      body.classList.contains('peek') || body.classList.contains('reading')`
      조건이라 **peek/reading 상태(이미 문서가 선택돼 패널이 최소한 26px
      걸쳐 보이는 상태)에서만** 동작한다. 손잡이(`#panelpeek`, "◂" 화살표,
      파란 accent 색)도 `body.peek #panelpeek{display:block}`이라 peek
      상태일 때만 보인다(`graphview.py:562-566`). 즉 **아무 문서도 선택 안 한
      "완전히 접힌" 상태에서는 화살표 손잡이 자체가 화면에 없다** — 사용자가
      원하는 "패널이 안 나온 상태에서 끌어서 열기"가 애초에 진입점이 없는 게
      근본 원인.
- [x] **수정**: CSS `#panel` 기본 상태를 "26px 걸쳐 보이기"로 바꾸고(기존
      `body.peek`에서만 걸치던 걸 idle 에도 적용), `body.reading`일 때만 전체
      노출. `#panelpeek` 손잡이도 기본 노출(데스크톱은 바깥 미디어쿼리 밖
      `display:none`으로 그대로 숨김). `attachDragSheet(#panel,...)`의
      `enabled` 제한(peek/reading만 허용) 제거 — idle 상태도 항상 드래그
      가능. `openPeekPanel()`이 `revealedDocId` 없어도(idle) 기본 힌트를 그대로
      전체 오픈으로 보여주게 확장 + `openMobilePanelUI()`로 히스토리 등록.
      드래그로 닫을 때(reading→걸침)는 `peek` 클래스를 잘못 붙이지 않도록
      `reading`만 벗김(CSS 기본값이 이미 걸침 모양이라 필요 없음).
- [x] **검증(로컬 mock `serve-api` + Playwright, 실제 클릭+뒤로가기)**: idle
      상태에서 `#panelpeek` `display:block` 확인(PASS) → 실제 클릭 →
      `body.reading` 전환 확인(PASS) → 실제 `page.go_back()` → idle 로 복귀
      확인(PASS). 첫 시도에서 데스크톱 기본 규칙(`#panelclose,#panelpeek
      {display:none}`, 미디어쿼리 밖)이 모바일 쪽에서 재정의가 안 돼 손잡이가
      실제로는 안 보이는 회귀를 실측으로 잡아 고침(display:block 명시 추가).
      **미검증(남은 과제)**: 실제 터치 드래그(마우스 클릭이 아니라 손가락으로
      끄는 제스처) 자체는 로컬에서 CDP 터치 드래그까지는 못 돌려봄 — 코드 경로는
      기존 peek 드래그와 동일(enabled 조건만 제거)이라 동작할 것으로 보이나,
      실기기에서 최종 확인 권장.

## 4. 폰에서 그래프가 느림 — 확대 시 프레임이 한 자리수(FPS)

**증상(원문)**: "폰에서는 그래프가 느린것같음. 확대몇번 해서 자세히보니
프레임이 한자리수가 나오는 수준."

- [x] **원인 확정(실측)**: 로컬에 합성 스트레스 데이터(엔티티 600·관계
      660 — 실제 운영 규모와 비슷하게 무작위 생성, `data/claire.db`는 전혀
      건드리지 않고 별도 임시 사본에만 적재) + `serve-api`(mock provider) +
      Playwright(모바일 뷰포트 390×844, CDP `Emulation.setCPUThrottlingRate`
      로 4배 슬로우다운 — 중급 폰 근사) + CDP Tracing(V8 CPU 프로파일)으로
      확대 동작(스크립트로 `net.moveTo({scale...})` 20회 반복)을 재현:
      **평균 5.4 FPS**(p50 116.7ms, worst 2.4초)로 사용자 보고와 일치. CPU
      샘플의 **~32%가 `_calculateForces`/`_getForceContributions`/
      `_placeInTree`**(barnesHut 반발력 계산 함수들)에 쓰이고 있었다 —
      `graphview.py:1140`(현재는 이동됨) 옵션에 안정화 후 `physics:false`
      전환 코드가 없어 물리 시뮬레이션이 무한히 계속 돌고 있던 게 확인됨.
- [x] **수정 1**: `net.once('stabilizationIterationsDone', () =>
      net.setOptions({physics:false}))` 추가 — 초기 레이아웃 안정화 뒤 물리
      끔. 검색 결과 뭉치기(`clusterMatches`)만 예외적으로 필요해 그 함수
      시작 시 `physics:true`로 다시 켜고, `unclusterEdges()`(정리) 끝에서
      다시 끔.
- [x] **수정 2(추가 개선)**: `interaction.hideEdgesOnZoom/hideEdgesOnDrag:true`
      추가 — 줌/드래그 중엔 엣지(660개) 그리기를 생략해 순간 부하 완화.
- [x] **검증(A/B 실측, 동일 스크립트)**: 물리 끄기 전(5.4 FPS) →
      후(21.7 FPS, p50 16.7ms) → `hideEdgesOnZoom` 추가 후(**28.5 FPS**,
      worst 750ms) 단계별로 개선 확인. 개선 후에도 활성 줌 도중 짧은 순간
      100~300ms대 프레임이 간간이 남아있지만(엣지·라벨 draw 비용), 줌을 멈추면
      즉시 16.6~16.7ms(60fps)로 안정 — 이전의 "계속 느림"과는 질적으로 다름
      (사용자가 보고한 "한 자리수 FPS"의 근본 원인인 상시 물리 시뮬레이션은
      해소).
- [x] `pytest tests/` 214건+서브테스트6 전체 통과(회귀 없음).

## 5. 폰 하단 패널(목록) 3단계 위치 지원 요청

**증상/요청(원문)**: "폰에서 하단 패널이 3단계로 위치가 가능하면 좋겠음.
1) 그래프 안보이게 완전히 펼치기, 2) 지금수준 유지하기, 3) 문서 검색 정도만
보일정도로 완전히 접기"

- [ ] **현재 상태**: `#docs`는 현재 collapsed(21%)/`listopen`(79%) 2단계뿐
      (`graphview.py:470,483` 근방). 3단계 스냅 포인트(완전펼침/중간/최소) 확장
      필요 — 신규 기능(버그 아님).
- [ ] **설계 필요**: 드래그 스냅 지점 3개로 확장(`attachDragSheet`의 `ratio`
      임계값을 2구간→3구간으로), 각 단계의 실제 높이(vh) 값 정의, 검색창은
      3번(최소 접힘) 상태에서도 항상 보이게 레이아웃 유지.

## 진행 상태

**0~4번 처리 완료(2026-07-24)** — 코드 수정 + 로컬 mock DB(`data/claire.db`는
전혀 안 건드리고 임시 사본만 사용) + `serve-api` + Playwright(실제 브라우저
클릭/뒤로가기, 4번은 CPU 스로틀링+CDP 프로파일)로 검증. `pytest tests/`
214건+서브테스트6 전체 통과, 회귀 없음.

**5번(하단 패널 3단계 위치)은 신규 기능 설계가 필요해 이번 라운드에서는
미착수** — 다음에 이어서 진행.

**미검증 남은 항목**: 3번의 실제 손가락 드래그 제스처(마우스클릭이 아닌
진짜 touch move)는 로컬에서 CDP 터치 드래그까지는 안 돌려봄 — 실기기에서
왼쪽으로 끌어 여는 동작 최종 확인 권장. 4번은 개선 폭은 실측했으나 실기기
체감(특히 저사양 기종)은 배포 후 재확인 필요.

## 시크릿 추가 조치 — scripts/backup_pull.sh 하드코딩 제거

0번 점검 이후 사용자가 `.sh` 파일도 다시 확인해달라고 요청 — `scripts/
backup_pull.sh`에 원격 LAN IP·SSH 사용자명(`blackan@192.168.1.8`)과 원격
절대경로가 하드코딩돼 있던 걸 발견(API 키/비밀번호 같은 자격증명은 아니고
SSH 키 없이는 접근 불가하지만, 내부 인프라 정보 노출). `deploy.sh`와 같은
`.env` 키(`DEPLOY_REMOTE`/`DEPLOY_PORT`/`DEPLOY_PATH`)를 읽도록 수정(값 해석
결과가 기존 하드코딩 값과 동일함을 확인) — 커밋 `4c0cc18`.
**주의**: 과거 커밋 이력에는 예전 하드코딩 값이 그대로 남아있음 — 지우려면
히스토리 재작성(force-push)이 필요해 이번엔 진행 안 함(사용자가 필요시 별도
요청).

## 배포 완료(2026-07-24)

커밋 `185c97f`(그래프뷰 버그 4건) + `4c0cc18`(backup_pull.sh 시크릿 제거) →
`./deploy.sh`(CI 게이트 214건+서브테스트6 통과) → 6컨테이너(bot/api/refresh/
recover/backup/expand) 재빌드+재기동 → 헬스체크 정상(`ok:true,
degraded:false, db:ok`, provider gemini, 문서 219·엔티티 602·관계 663,
최근 백업 `claire-20260724-133656.db`). `git push` 완료.
