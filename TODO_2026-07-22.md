# 2026-07-22 폰 UI 버그 4건 — TODO

사용자가 구두로 전달한 항목을 먼저 정리(협약대로 원인 파악 전이라 증상만).
쉬운 것부터 하나씩 조사 후 처리.

## 1. 폰에서 크게읽기 시 글 아래가 잘림

**사용자 증상(원문)**: "폰에서 크게읽기 시 글 아래가 잘림"

- [x] **원인 조사(2026-07-22)**: 로컬 mock DB + Playwright 헤드리스로 긴 문서를
      크게읽기로 열어봤으나 헤드리스 환경에선 재현 안 됨(`#rbody` 자체 스크롤이
      바닥까지 정상 도달). **정직히 기록**: 헤드리스 Chromium은 모바일 브라우저의
      "주소창 표시/숨김에 따라 실제 보이는 높이가 달라지는" 동적 뷰포트를 재현 못 함
      — `#reader .sheet{height:100vh}`(모바일)가 이 문제의 유력 후보(매우 흔한
      모바일 웹 버그 클래스): `100vh`는 주소창이 접힌 최대 상태 기준이라, 주소창이
      떠 있는 상태에서 실제 보이는 높이보다 sheet가 더 크게 계산되어 하단이
      화면 밖으로 밀려남. 아이폰이면 홈 인디케이터 세이프에어리어도 겹쳐 하단
      패딩(28px)보다 더 가릴 수 있음.
- [x] **수정**: `#reader .sheet` 모바일 규칙에 `height:100dvh`(동적 뷰포트,
      100vh 폴백 유지) + `.rbody` 하단 패딩에 `env(safe-area-inset-bottom)` 추가.
- [x] **더 정확한 원인 발견 + 수정(2026-07-22, Playwright로 확정 재현)**: `#reader
      .sheet`가 `box-sizing` 지정 없이(기본 `content-box`) `height:100vh` +
      `padding:0 0 28px` + `border:1px solid`을 같이 쓰고 있었다 — `height`는
      content box만 채우고 padding/border가 그 위에 **추가**로 붙어, 모바일에서
      실제 렌더 높이가 뷰포트보다 **약 30px 더 커짐**(Playwright 측정:
      `sheetH=874px` vs 뷰포트 `844px`, 정확히 padding-bottom(28px)+border(2px)만큼
      초과). 이 초과분이 화면 아래로 밀려나 "글 아래가 잘림"으로 보임 — 헤드리스
      에서도 100% 재현되는 확정적 원인(주소창 이론과 별개로 이게 실제 원인).
      `#reader .sheet`에 `box-sizing:border-box` 추가로 해결.
- [x] **검증**: Playwright로 수정 전/후 비교 — 수정 전 `sheetH=874>844`(초과),
      수정 후 `sheetH=844===readerClientH`(정확히 일치, 초과 없음). 긴 문서(15개
      섹션, 4300자) 크게읽기로 열어 `#rbody` 끝까지 스크롤 → 마지막 줄까지 완전히
      보임 확인. `100dvh`(주소창 이슈 대비 방어적 보강, 100vh 폴백 유지)도 함께
      적용. `pytest tests/` 214건+서브테스트6 전체 통과(회귀 없음).

## 2. 폰에서 목록 열고 기사 선택 시 그래프 이동 기준점이 틀림 → 노드 안 보임

**사용자 증상(원문)**: "폰에서 목록열고 기사 선택하면 그래프가 기사관련
노드로 이동되는데, 화면 중앙의 기준이 목록 열기로 인해 작아진 그래프
화면기준이 아니라, 작아지기 전 큰 화면 기준의 중심으로 이동해서, 결국
노드가 안보임"

- [x] **원인 확정(Playwright로 재현)**: `#docs`(목록)는 `position:absolute`
      오버레이라 `#netwrap`(그래프 컨테이너)의 실제 DOM 박스 크기는 목록이
      펼쳐져도 줄어들지 않는다(그래프 리사이즈 버그 재발 방지 설계, 의도된
      부분). `fitToDocNodes()`가 `net.fit()`으로 캔버스 "전체" 기준 중앙을
      잡으므로, listopen 땐 캔버스의 약 79%가 목록에 가려진 채로 그 안쪽에
      중심이 잡혀버림. 측정: 보이는 영역은 `[0,147.7px]`인데 fit 후 노드는
      `y≈363.5px`(가려진 영역 안)에 위치 — 정확히 사용자가 말한 증상과 일치.
- [x] **수정**: `fitToDocNodes()`에서 모바일+listopen이면 `net.fit()`을 즉시
      (무애니메이션)로 먼저 계산한 뒤, `#docs`가 가리는 높이만큼 `net.moveTo()`의
      `offset`으로 보정해 "실제 보이는 영역"의 중앙으로 애니메이션 이동.
- [x] **검증**: Playwright로 노드의 실제 DOM 좌표를 직접 측정 — 수정 전
      `y=363.5`(보이는 영역 밖), 수정 후 `y≈71`(보이는 영역 `[0,147.7]` 안,
      중앙에 가까움). `pytest tests/` 전체 통과.

## 3. 폰에서 목록 열고 크게읽기 중 뒤로가기 1번 누르면 둘 다 닫힘

**사용자 증상(원문)**: "폰에서 목록 열고 크게읽기 중 뒤로가기 한번 누르면,
기대되는건 크게읽기만 닫히는건데, 둘다 닫힘"

- [x] **원인 조사(정직히 기록 — 헤드리스로 재현 못 함)**: `uiStack`
      (`pushUIState`/`closeTopUIState`/`popstate`) 코드를 직접 검토하고
      Playwright `page.go_back()`으로 "목록 열기→크게읽기 열기→뒤로 1번"을
      반복 재현해봤으나, 코드는 스펙대로 정확히 동작함(뒤로 1번 → 크게읽기만
      닫히고 `uiStack`은 `['list']`로 목록은 유지) — **버그를 재현하지 못했다.**
  - 조사 중 **별도의 진짜 버그**를 하나 발견해 같이 고침: `attachDragSheet`의
    `pointermove` 핸들러가 `pointerdown` 이전에 들어온 미아 `pointermove`(예:
    클릭 전 커서 이동)를 `startX/Y` 기본값(0,0) 기준으로 델타 계산해 탭 하나를
    거대한 드래그로 오인 → `#docs`에 `height`/`transition:none` 인라인 스타일이
    눌어붙어 목록이 `listopen` 클래스가 붙어도 실제로는 예전 높이 그대로인
    채 멈추는 버그(Playwright 마우스 클릭으로 확정 재현: 클릭 1번 후
    `el.style`에 `height:276px`(38%, collapsed) 눌어붙음). `down`(눌림중) 가드
    추가로 수정, 재현 스크립트로 수정 전/후 비교 확인.
  - **미확정**: 이 눌어붙음 버그가 사용자가 겪은 "뒤로가기 시 목록도 같이
    닫힘"으로 보이는 원인일 수 있다(목록이 실제로는 계속 collapsed 높이로
    멈춰 있었다면, 사용자 입장에선 "목록이 안 열려있었던 것"처럼 보여
    뒤로가기 후 "둘 다 닫힌 것"으로 오인했을 가능성) — 다만 이건 추정이고
    확정 아님. 위 pointermove 가드 수정으로 이 경로는 막혔으니, **이 수정
    배포 후에도 재발하면 알려달라**(정확한 기기/브라우저·탭 순서 재현 정보
    필요).

## 4. 폰에서 목록 열고 드래그 핸들 끌면 스크롤로 인식되어 페이지 새로고침(pull-to-refresh)

**사용자 증상(원문)**: "폰에서 목록 열고, 상단에 잡아서 끌고 내리는 UI가
있는데, 실제로 잡고 내리려고 하면, 스크롤로 인식되는지 웹페이지 전체
새로고침이 됨"

- [x] **원인 확정**: `#docs` 전체에 걸린 드래그 핸들러(`attachDragSheet`)가
      `enabled: () => !listopen`(collapsed일 때만 동작)로 짜여 있었다 — "펼친
      뒤엔 목록 내부 자체 스크롤을 살려줘야" 해서 의도된 설계였지만, 그 결과
      **listopen 상태에서 손잡이(#draghandle)를 끌면 우리 코드가 아예 반응을
      안 해** 터치가 그대로 브라우저 기본 제스처(pull-to-refresh)로 새어나감.
- [x] **수정**: `#draghandle`(작은 전용 영역, 리스트 아이템을 포함 안 해 목록
      스크롤과 안 겹침)에 listopen 전용 별도 `attachDragSheet` 추가 — 아래로
      50% 넘게 끌면 `closeTopUIState('list')`로 접힘(뒤로가기 스택과 일관).
      `touch-action:none`도 핸들에 추가해 브라우저가 애초에 기본 제스처로
      판정하기 전에 우리 코드가 가로채게 함(기존 `#docs`/`#net`에 이미 쓰던
      것과 같은 검증된 패턴, TODO_2026-07-20 참고).
- [x] **검증(실제 터치 이벤트로, 합성 마우스 아님)**: 처음엔 Playwright
      `mouse.move`로 테스트했으나 마우스 포인터는 터치처럼 암묵적 캡처가
      안 돼(엘리먼트 밖으로 나가면 이벤트가 안 잡힘) 거짓 실패가 나서,
      TODO_2026-07-20 교훈대로 CDP `Input.dispatchTouchEvent`(진짜 터치)로
      재시도 — 300px 아래로 끌기 → `listopen` 클래스 정상 제거(접힘),
      `uiStack` 정상 비워짐. 40px(50% 미만) 짧은 끌기는 안 닫히고 원위치
      유지(스냅백) 확인. `pytest tests/` 전체 통과.

## 진행 상태

**전부 처리 완료(2026-07-22)** — 로컬 mock DB + `serve-api` + Playwright
(실제 렌더링, 1번은 CSS 측정치로 확정 재현, 2/4번은 DOM 좌표·실제 터치
이벤트로 확정 재현, 3번은 재현은 못 했으나 원인이 될 수 있는 별도 버그
발견해 수정)까지 검증. `pytest tests/` 214건+서브테스트6 전체 통과, 회귀
없음.

**배포 완료**: 커밋 `1a43b00` → `./deploy.sh`(CI 게이트 214건+서브테스트6 통과)
→ 6컨테이너(bot/api/refresh/recover/backup/expand) 재빌드+재기동 → 헬스체크
정상(`ok:true, degraded:false, db:ok`, 문서 219·엔티티 602·관계 663, 최근
백업 정상). **3번(뒤로가기)은 실기기에서 재확인 필요** — 재발하면 정확한
기기/브라우저·탭 순서를 알려주면 좋음.
