# 2026-07-20 모바일 슬라이드 완전 고장 — TODO

## 1. 모바일 UI 전반 사용 불가, 특히 영역 간 슬라이드(드래그시트) 고장

**사용자 증상(원문)**: "모바일 화면 너가 열어서 알아서 고쳐봐. 전혀 못쓰겠네. 특히 영역
간 슬라이드 같은거. 너가 저번에 완전히 고장내놓은듯."

**배경 조사**: 착수 전 `git status`/원격 배포 상태를 먼저 확인.
- `src/claire/graphview.py`가 로컬에 **커밋 안 된 채(untracked 아님, modified 상태)로
  5일째 방치**돼 있었음. 이 uncommitted diff는 `TODO_2026-07-15.md` #9번 항목
  ("확장→리빌→드래그 추종 제스처로 개편")의 구현분 — `attachDragSheet()` 헬퍼(포인터
  추종+50% 스냅) 신설, `#docs`/`#panel` 양쪽에 적용.
- 원격 배포 컨테이너(`~/claire_bible/src/claire/graphview.py`, `192.168.1.8:2222`)의
  파일과 **로컬 uncommitted 작업트리 파일이 바이트 단위로 동일** — 즉 이 미완성/미검증
  상태가 이미 `./deploy.sh`로 배포되어 사용자가 실제로 보고 있는 화면이었음. 반면 로컬
  `git HEAD`(마지막 커밋 `d6ed17e`)는 이보다 오래된 버전 — **git 커밋과 실제 배포가
  분리된 채 방치**돼 있었다(작업 협약 위반 소지 — 정리 필요, 아래 "남은 것" 참고).
- `TODO_2026-07-15.md` 자체 기록도 모순적: 헤더는 "이번 세션 구현은 안 함, 다음
  세션이 이어받기"라고 적어놓고 바로 아래 절에는 "**구현(이번 세션)**"+Playwright
  검증 통과 기록이 있고, 마지막 줄엔 "**배포: 아직 안 함 — 사용자 확인 후 진행
  예정**"이라 적혀 있음 — 그런데 실제로는 배포까지 됐고 문서는 갱신 안 됨. 즉 어느
  시점에 배포가 사용자 확인 없이(또는 확인됐으나 기록이 안 됨) 진행됐고, 그 상태로
  최소 5일간 실사용자가 고장난 화면을 사용해온 것으로 보임.

**재현(로컬 mock 서버 + 실제 터치 파이프라인)**: 과거 세션들의 검증은 전부
`page.evaluate()`로 합성 `PointerEvent`를 `dispatchEvent`한 것 — 이는 JS 리스너는
타지만 브라우저의 **`touch-action` 기반 네이티브 스크롤/제스처 판정 자체를 아예 안
거친다**. 그래서 그 방식으로는 "브라우저가 우리 드래그 제스처를 가로채 가는" 클래스의
버그를 원천적으로 재현할 수 없었다(TODO_2026-07-15.md도 이미 이 한계를 자인한 바 있음
— "실기기 재현이 안 된 상태… 다음 세션은 CPU 스로틀링+실제 touch 이벤트 시퀀스부터").
이번엔 Chromium을 CDP(`Input.dispatchTouchEvent`)로 직접 조작해 **진짜 터치 입력
파이프라인**으로 재현.

- [x] **원인 확정**: `#docs`(문서 목록 바텀시트)·`#panel`(상세 패널)에 적용한
      `attachDragSheet()`는 손가락 이동이 8px 문턱을 넘은 뒤에야(`pointermove` 안에서)
      `preventDefault()`를 호출한다. 그런데 두 요소 모두 CSS `touch-action`이 기본값
      (`auto`)이었다 — 스펙상 브라우저는 그 문턱보다 먼저(첫 `touchmove`에서) 이미
      "이건 세로/가로 스크롤 제스처"로 판정해버리고, **한 번 방향이 잠기면 이후
      `preventDefault()`는 효과가 없다.** 결과: 실제 220px 스와이프 중 우리 JS에
      전달된 이동량은 19px뿐 — 50% 스냅 문턱을 못 넘어 목록이 안 열리거나, 손가락을
      따라오지 않고 뚝뚝 끊기는 느낌(사용자가 "전혀 못쓰겠다"고 느낀 원인).
      실측: mock 서버(문서 8개, 즐찾 3개) + Chromium(iPhone 13 에뮬레이션) + CDP 실제
      터치 시퀀스로, 수정 전 `#docs` 195px 스와이프 → `listopen` 진입 실패(높이
      210→229px만 변함, 문턱 미달) 재현.
- [x] **수정**: `src/claire/graphview.py` 모바일 미디어쿼리에 두 줄 추가.
      - `body:not(.listopen) #docs{touch-action:none}` — 드래그가 활성인 collapsed
        구간에서만 브라우저의 기본 제스처 판정을 꺼서 모든 이동량이 우리 코드로 온다
        (`#net` 캔버스에 이미 쓰던 `touch-action:pan-y` 패턴과 같은 해법 — 새 기법
        아님, 이 코드베이스에 이미 있던 정석을 놓쳤던 것).
      - `body.peek #panel, body.reading #panel{touch-action:pan-y}` — 가로 드래그는
        우리가 가져가되, 패널 내용(요약 등)의 세로 스크롤(`#panel{overflow:auto}`)은
        살려둠(`pan-y` 허용).
      - listopen 이후·collapsed 아닐 때는 원래 값(`auto`)으로 자동 복귀(내부 리스트
        스크롤 필요).
- [x] **두 번째 원인 발견(`#panel` 가로 드래그 검증 중 발견, 더 치명적)**: `#panel`
      드래그 검증을 위해 `elementFromPoint`로 peek 상태의 "26px 걸친 조각" 좌표를
      뷰포트 전체에서 스캔했는데 **한 곳에서도 `#panel`을 찾지 못함** — 걸친 조각
      자체가 화면 밖에 있었다.
      **원인**: `#panel`의 모바일 미디어쿼리 오버라이드는 `width:100%`와
      `padding-top:52px`만 재정의하고, 데스크톱 기본 규칙의 `padding:14px 16px`
      (좌우 32px)은 그대로 상속받는다. 전역 `box-sizing`이 기본값(`content-box`)이라
      padding이 width에 더해져 **실제 렌더 너비가 422px(뷰포트 390px + 32px)**로
      튀어나갔다. peek 상태 `translateX(calc(100% - 26px))`이 이 부풀려진 너비
      기준으로 계산되니 26px 걸친 조각이 뷰포트 오른쪽 밖으로 완전히 밀려나 화면에
      전혀 안 보였다(reading 완전 오픈 상태에서도 오른쪽 32px가 뷰포트 밖으로
      넘침). **"영역 간 슬라이드"가 근본적으로 안 되는 문제의 핵심은 사실 이거였다**
      — touch-action 문제는 #docs 목록엔 실재했지만, #panel 쪽은 애초에 만질 대상
      자체가 화면에 없었으니 드래그/탭 둘 다 안 먹히는 게 당연했다.
      **수정**: `#panel` 모바일 규칙에 `box-sizing:border-box` 추가 — padding이
      100% 안에 포함되어 실제 렌더 너비가 뷰포트와 정확히 일치.
- [x] **검증(같은 CDP 실제 터치 방식으로 재실측, 두 수정 모두 반영 후)**:
      - `#docs`: 실제 220px 위로 스와이프 → `body.listopen` 정상 진입, 높이
        207px→430px(≈79%, openPx 문턱 정상 도달).
      - `#panel` 너비: peek 상태 `translateX` 364px(=390−26, 뷰포트와 정확히 26px
        겹침 — 수정 전엔 396px로 전혀 안 겹쳤음). `elementFromPoint`로 뷰포트 안에서
        `#panel` 정상 탐지(y=40).
      - `#panel` 가로 드래그: peek 걸친 조각을 실제로 왼쪽 스와이프 →
        `body.reading` 정상 진입, `translateX(0)`(완전히 열림) 확인.
      - `pytest tests/test_graphview.py`(12건) + `scripts/ci.sh` 전체(205건+서브테스트
        6건) 통과.
- [x] **배포**: `./deploy.sh` 완료.
- [x] **git 커밋 정리**: 이번 세션 시작 시점에 이미 5일치 미커밋 대형 diff가 있었음
      (원인: 과거 세션이 배포까지 하고 커밋을 안 남김). 사용자 확인("핸드오프,커밋,배포")
      받고 정리 — `src/claire/graphview.py`(#5~#9 모바일 개편 + 이번 두 수정) 커밋,
      `CLAUDE.md`·`TODO_2026-07-15.md`·`TODO_2026-07-20.md` 커밋.

## 핸드오프

이번 세션 필수 작업 전부 완료(원인 조사·수정·실측 검증·CI 통과·배포·커밋). 다음
세션 이어받을 필수 항목 없음. 남은 건 아래 2건 — 급하지 않음, 사용자 확인 위주.
- [ ] **실사용자 재확인**: 실제 기기(iOS Safari 등)에서 확인 필요 — 이번 검증은
      Chromium(iPhone 13 에뮬레이션)의 실제 터치 파이프라인 기준이라 Safari 고유
      동작까지는 담보 못함(과거 #3 GPU 글리치 오진 사례처럼, WebKit 전용 차이가
      남아있을 가능성 배제 못 함).
- [ ] **재발 방지(논의 필요)**: "구현→검증(합성 이벤트)→배포"가 여러 세션에 걸쳐
      반복되며 실기기에서 계속 깨지는 패턴이 이번까지 최소 3번째(#3 GPU 글리치 오진,
      #4/#9 실기기 미재현 인정, 이번 touch-action). 합성 dispatchEvent 검증은 이
      부류의 버그를 구조적으로 못 잡는다는 게 이번에 확인됐으므로, 앞으로 터치
      제스처 관련 변경은 **CDP 실제 터치 파이프라인 방식**(이번에 만든
      `mobile_touch_test.py` 패턴)을 기본 검증 수단으로 삼는 게 나아 보임(제안,
      사용자 확인 필요).
