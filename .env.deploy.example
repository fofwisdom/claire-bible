# Copy to .env.deploy. Deployment control settings never enter app containers.
# Remote deployment is always CLAIRE_ENVIRONMENT=production.
DEPLOY_REMOTE=
DEPLOY_PORT=22
# 배포 대상 경로 (예: private/claire 또는 /srv/private/claire)
DEPLOY_PATH=
DEPLOY_ENV_SYNC=if-missing
DEPLOY_ACTION=update
SKIP_CI=0
