# 2026-07-13 원격 점검 + 수정 TODO

사용자가 보고한 12건. 원격(`ssh -p 2222 blackan@192.168.1.8`) 실제 DB/컨테이너 점검 후
세부화. 상태: `[ ]` 미착수 · `[~]` 진행중 · `[x]` 해결(배포/검증 완료) · `[보류]` 후순위(사용자 지정).

## 1. YouTube 실패 흔적 + 재시도 불가 (`YouTube Ae9q7KsRbuI`)
**원인 확인(원격 DB raw_inbox#199)**: fetch(transcript)는 성공해 문서가 이미 적재됨
(`doc_edc5000ff71a`, title="YouTube Ae9q7KsRbuI" — 실제 영상 제목을 가져오는 코드가
아예 없음, 항상 `f"YouTube {vid}"`). 그 다음 단계인 Gemini 추출이
`429 monthly spending cap exceeded`(실제 결제 계정 한도 — 재시도로 안 풀림)로 실패 →
recover-loop 가 5회 재시도 후 `status='failed'`(영구실패)로 굳힘.
재전송 시 "이미 들어간 링크" = 문서가 이미 있어(content_hash 일치) dedup①에 걸리는 것 —
정상 동작이지만, 사용자 입장에선 재추출할 방법이 없어 막힌 것처럼 보임.
**결제 한도 자체는 우리가 못 고침(사용자의 AI Studio 계정 조치 필요)**. 우리가 고칠 것:
- [x] YouTube 실제 영상 제목 가져오기(oEmbed, API 키 불필요) — 해시같은 제목 문제(9번)와 공통 원인
- [x] 텔레그램에서 실패/영구실패 항목을 보고 재시도할 수 있는 명령 추가(2번과 통합 해결)
- [x] **원격 배포 후 실증**: `svc.retry_inbox(199)` → error=None(추출 성공, 결제한도 문제
      해소됨/일시적이었던 것으로 보임). entities 519→522, `degraded:false` 로 완전 정상화.
      (기존 문서라 제목 자체는 재추출만으론 안 바뀜 — 소급 개선은 범위 밖, 향후 신규
      유튜브 적재부터 정상 제목 확인 가능)

## 2. 영구실패 알림에도 점검 수단이 텔레그램에 없음
**확인**: 알림이 `claire status`/`replay-failed` 를 안내하지만 텔레그램 봇 명령은
`/help /status /search /web /start` 뿐 — CLI 전용, SSH 없이는 무용지물.
- [x] 텔레그램 `/failed` (최근 실패/영구실패 목록 + 사유) 추가
- [x] 텔레그램 `/retry <inbox_id>` (해당 건 재시도 — extract 만 실패시 재추출, fetch 부터 실패시 재적재) 추가
- [x] 봇 메뉴(`set_my_commands`)에 등록

## 2b. 실제 thin-content 실패 사례: uniclawbench.github.io/#/home
**확인**: JS SPA(해시 라우팅) — static/discourse/scrapling(curl-cffi, 무브라우저) 다 실패.
기존 4번째 폴백 `_fetch_stealth` 는 Scrapling `StealthyFetcher`(Playwright) 였는데
**원격 컨테이너에 브라우저 바이너리 미설치**라 항상 빈 결과 → 영구실패 확정.
**사용자 결정**: Playwright 는 쓰지 말고 다른 걸 실험. `nodriver`(CDP 직접 제어, Chrome/Chromium
바이너리만 필요, playwright/patchright 의존성 없음)로 로컬 실험 → uniclawbench 실제 렌더링
성공(3.5초, 제목 "UniClawBench", 본문 정상 확보) 확인.
- [x] `_fetch_stealth`(Scrapling/Playwright) → `_fetch_cdp`(nodriver) 로 교체
- [x] pyproject `stealth` extra 에 `nodriver` 추가
- [x] Dockerfile 에 `chromium` apt 패키지 설치(6컨테이너 공통 이미지)
- [x] 단위 테스트 갱신(`_fetch_stealth` 참조 → `_fetch_cdp`)
- [x] **로컬 실증 검증 완료**: `fetch_web('https://uniclawbench.github.io/#/home')` →
      title="UniClawBench", via="cdp", 본문 2969자(리더보드 내용 포함) 정상 확보
- [x] **원격 배포 후 실증(2026-07-13)**: `svc.retry_inbox(202)` → 실제 렌더링 성공,
      title="UniClawBench", Gemini 요약까지 정상 생성(신규 문서 189번째로 적재).
      `svc.retry_inbox(203)` → 202 와 동일 콘텐츠로 정확히 duplicate 판정(dedup 정상).

## 3. 검색 자동완성 딜레이 부족(지우고 다시 입력 중 자꾸 실행)
**확인**: `oninput="onSearchInput(this.value)"` — 디바운스 없이 매 키입력마다 실행.
- [x] 이미 350ms debounce 는 구현돼 있었음(2026-06-26 커밋) — 사용자 요구는 "그래도
      지우고 재입력 중 걸린다" = **더 늘려야** 한다는 뜻으로 재해석, 550ms 로 상향

## 4. 전체 리로드 없이 새 글 반영
**결정**: 가벼운 주기 폴링(20~30초). 서버 부하 거의 없음.
- [x] 기존 `/stats`(문서 개수) 25초 주기 폴링 + 변경 시에만 기존 `refreshGraph()`
      (append-only 병합, 이미 조사 기능용으로 있던 함수 재사용) 호출. 변화 없으면 요청만
      가고 렌더링 0회 — 서버 부하·화면 깜빡임 최소화.

## 5. MCP 노출 (에이전트가 API로 지식 참조) — **후순위(사용자 지정)**
- [보류] 설계만: 6번(읽기전용 토큰) 이 이 작업의 기반이 됨. 실제 MCP 서버 구현은 별도 세션.

## 6. 읽기 전용 공개 토큰
**결정**: owner bearer 와 별개, GET 계열(검색/그래프/노드상세/문서목록)만 허용, 쓰기(ingest·
dedup-merge·공유링크발급 등)는 차단.
- [x] `CLAIRE_READONLY_TOKEN` 설정 추가(config.py + .env.example)
- [x] 서버 gate 에 `READONLY_PATHS` 화이트리스트(`/ /graph /node /documents /document
      /search /stats`) — 경로 기반 경계(핸들러 신뢰 아님), 쓰기/비용(ingest·dedup/merge·
      share·synthesize·research)는 애초에 도달 불가
- [x] **실증 검증(curl, mock provider, 임시 포트)**: 토큰無=전부 404 / readonly 토큰=
      graph·stats·documents·search 200, ingest·dedup/merge·dedup(조회)·share 전부 404 /
      owner 토큰=ingest 도달(400=검증오류, 인증통과) 확인

## 7. 문서 즐겨찾기 + 숨기기
**결정**: 숨기기는 문서 목록에서만 숨김(그래프 엔티티/관계는 그대로 유지). 즐겨찾기는
좌측 문서 리스트 상단 고정 섹션. 각 문서 리스트 아이템 최소 높이 필요(즐찾 많아질 때 대비).
- [x] DB: `documents.pinned`, `documents.hidden` 컬럼(SCHEMA_VERSION 7→8, `_ensure_column`)
- [x] 서버: `POST /document/pin`, `POST /document/hide` 토글 엔드포인트(소유자 인증)
- [x] UI: 좌측 리스트 상단 "⭐ 즐겨찾기" 섹션(최대높이 32%+자체스크롤) + 각 문서 아이템
      ⭐(즐겨찾기)/📖(읽기)/🙈(숨기기) 3버튼 + `.docitem{min-height:34px}` + 숨김 문서는
      "🙈 숨김 N개 보기" 토글로 접어두되 언제든 펼쳐 숨김해제 가능(완전 유실 방지)
- [x] **실증 검증(Playwright, mock provider, 문서 5건 시드)**: ⭐ 즐겨찾기 섹션 분리 렌더,
      🙈 숨김 문서 목록 제외+토글로 재노출, `togglePin()` 서버 왕복 후 `/documents` 재조회로
      `pinned:1` DB 반영 확인. JS 문법 `node --check` 통과.

## 8. 동일 내용 여러 번 작성 시 자동 병합 — **후순위(사용자 지정)**
- [보류] 기존 근사중복(MinHash) 엔진은 URL 재적재 대상. "여러 번 작성"이 텍스트 메모/노트
  반복 작성을 의미한다면 별도 설계 필요(의미 유사도 기반) — 다음 세션에서 사용자와 범위 확정.

## 9. 유튜브 추가 시 제목이 해시(영상ID)로 잡히는 문제
**확인**: `fetch_youtube` 가 제목을 항상 `f"YouTube {vid}"` 로 하드코딩 — "종종"이 아니라
**항상**. 1번과 원인 동일.
- [x] oEmbed(`https://www.youtube.com/oembed?...`, API 키 불필요)로 실제 제목 가져오기,
      실패 시에만 기존 폴백 유지

## 10. 폰에서 그래프 핀치 줌이 안 됨
**확인**: Mac 트랙패드 휠 점프 버그(2026-06-24) 고치며 `interaction.zoomView:false` 로
꺼버렸는데, vis-network 는 이 옵션이 **휠 줌과 핀치 줌을 함께 제어** — 그래서 커스텀 휠줌
코드(`setupWheelZoom`)만 추가되고 모바일 핀치 경로는 대체 없이 죽어있었음(회귀).
- [x] 커스텀 핀치 제스처는 vis 내부 hammer 인스턴스와 이벤트 충돌 위험이 커서(hammer 가
      #net 자체에 바인딩 — capture 로도 못 가로챈다는 걸 실측 확인) **+/- 줌 버튼**으로 대체
      (사용자도 "터치나 버튼추가나" 로 버튼을 대안 제시). `#net`→`#netwrap`(위치기준자)+
      `#net`(vis 컨테이너, init 시 innerHTML 이 지워짐 — 실측 확인)+`#zoomctl`(형제, 살아남음)
      로 구조 분리, 모바일 폭에서만 표시(`@media max-width:820px`).
- [x] **실증 검증(Playwright)**: 모바일 뷰포트(390×844)에서 버튼 클릭 시 scale 0.371→0.463
      (확대)→0.297(축소×2) 정상 변화, 데스크톱(1280×800)에서는 버튼 `display:none` 확인.

## 11. 폰에서 문서선택 후 그래프 드래그 시 엉뚱한 위치로 리셋
**분석**: `selectDoc()` 가 `net.fit()` 애니메이션 직후 `mobileScrollTo('panel')`
(페이지 스무스 스크롤)을 호출 → 스크롤 중 모바일 브라우저 주소창이 접히며 뷰포트 높이가
바뀌고 `ResizeObserver` 가 `relayout()`(setSize+redraw)을 fit 애니메이션 도중에 실행,
카메라 상태가 깨진 채로 다음 터치팬이 시작되는 것으로 추정(실기기 재현 불가 — 방어적 수정).
- [x] `relayout()` 에 드래그/애니메이션 중(`netBusy`) 스킵 가드 + 사이즈 변화<1px 면 재실행
      안 함(churn 방지). `net.fit`/`net.moveTo(animation)`/`dragStart` 시 busy=true —
      **`animationFinished` 이벤트만 믿으면 실측상 항상 발화하지 않아(0개 노드 fit 등)
      영구 스킵 회귀 위험**이 있어 700ms 타임아웃 안전망을 병행(반드시 풀림 보장).
- [x] **Playwright 실증**: fit 애니메이션 직후 busy=true, 700ms 안전망으로 확실히 false
      복귀 확인, size 불변 시 relayout 스킵 확인. (근본원인은 실기기 재현 불가 — 방어적
      수정이며 100% 확정은 아님, 사용자 실기기 재확인 권장)

## 12. 이미지가 외부링크만 저장됨(원본 유실 위험)
**결정 필요 없음(설계는 명확)**: 현재 `meta.images` 는 원격 URL만 저장 — 원본이 삭제되면
영구 유실.
- [x] `store/raw.py download_images()`(artifact 패턴과 동일) — 각 이미지 후보를 내려받아
      `data/images/<doc_id>_<i>.<ext>` 저장 + meta 에 `"local"` 경로 부여. 개별 실패(403·
      404·비이미지·8MB초과·네트워크오류)는 그 이미지만 원본 url 유지(적재 안 막음).
      **실측**: 위키미디어는 기본 UA 로 403(브라우저 UA 추가해도 여전히 403 — 이 샌드박스
      IP 의 robot-policy 차단으로 추정, 코드 문제 아님. GitHub raw 컨텐츠로는 실제 다운로드
      성공 확인) → 봇차단 CDN 도 실패 시 원본 url 폴백이 정확히 동작함을 오히려 실증.
- [x] `ingest()` 신규/in-place갱신(dedup②) 두 경로 + `refresh_document()` nochange/변경
      두 경로 모두 연결(`pipeline._download_doc_images` 공용 헬퍼).
- [x] 서버 `GET /image?p=images/<file>` 로 로컬 파일 서빙 — 경로 화이트리스트 정규식
      검증(트래버설 불가), **공개**(PUBLIC_PATHS — 원래도 공개 웹의 이미지였고 `/p` 공유
      열람에서도 보여야 해서). `render_detail`/mock 모두 `local` 있으면 `/image?p=...`,
      없으면 원본 url 로 마크다운 작성.
- [x] **실증 검증**: 유닛(성공/404/비이미지/용량초과/네트워크예외 4종, 네트워크 없이 fake
      httpx.Client) + **실 네트워크 E2E**(GitHub raw PNG 8991바이트 다운로드→로컬 저장→
      DB meta local 반영→mock detail 이 `/image?p=...` 사용) + **실 서버 라우트**(curl,
      바이트 일치 200, traversal/확장자불일치/미존재 전부 404, 무인증 통과=공개 확인).
      **교훈**: 첫 시도(위키미디어)가 봇차단으로 실패해 실패-폴백 경로도 같이 실증됨.
- [x] **원격 컨테이너 실증**: `download_images()`를 원격 claire_api 컨테이너 안에서 직접
      실행 → GitHub raw 이미지 실제 다운로드·로컬 저장 성공(로컬 샌드박스와 별개로 원격
      네트워크에서도 정상 동작 확인).
- [ ] (선택, 별도 판단) 기존 문서 이미지 소급 백필 — 비용/트래픽 커서 사용자 결정 시
      `claire backfill-images` 확장으로 나중에 진행 가능

---
## 진행 로그
- 2026-07-13: 원격 점검 완료(3건 실패 확인: youtube 429 결제한도 1건, uniclawbench thin 2건).
  코드 조사로 9개 항목 원인 특정. 사용자에게 4개 설계 결정 확인(SPA=nodriver 실험,
  읽기토큰=GET전용 별도토큰, 실시간=폴링, 숨기기=목록만).
- 2026-07-13(계속): 10개 항목(1,2,2b,3,4,6,7,9,10,11,12) 코드 수정 완료 — 각 항목 실제
  네트워크/DB/Playwright/curl 실증 검증까지 마침. 2개는 후순위 보류(5 MCP, 8 자동병합,
  사용자 지정). 로컬 pytest 180→183개 전부 통과, `scripts/ci.sh`(uv.lock 동기+테스트)
  통과. **미완**: 원격 배포 + 원격 실증(uniclawbench 재시도, 기존 failed 2건 정리) —
  커밋/배포는 사용자 확인 후 진행(작업 협약: 정직·단계적, 배포는 되돌리기 어려운 작업).
  #11(모바일 드래그 리셋)은 방어적 수정이라 실기기 재확인 권장.
