# 2026-07-14 원격 점검 + 수정 TODO

사용자가 오늘 발견한 큰 버그 포함 5건. 상태: `[ ]` 미착수 · `[~]` 진행중 ·
`[x]` 해결(배포/검증 완료) · `[보류]` 후순위(사용자 지정).

## 1. "we married" 문서가 엉뚱하게 다른 문서에 흡수 병합됨 (큰 버그)
**사용자 증상**: "we married" 문서 요약을 요청했는데 엉뚱하게 다른 문서에 흡수되어버린
것 같다. 의도했던 건 A 링크를 보냈을 때 그로부터 파생된 A`, A`` 링크들이 비슷한 내용이면
합치는 것(=onehop 체인 한정, [[TODO_2026-07-13.md]] #8)이었는데, 지금 보면 **부모-자식
관계가 없는 기존 문서**와도 비교해서 비슷하다고 병합한 것 같다. 너무 과했고, 애초에 합칠
문서도 아니었다고 판단.
**의심 지점**: 어제(2026-07-13) 배포한 `judge_research` `same_subject` 판정(onehop 병합,
커밋 `91c62bd`)이 설계 범위(onehop 체인 한정)를 벗어나 발동했거나, 판정 자체가 너무
관대할 가능성. 원격 DB에서 실제 병합 이력(`extra_sources`/raw_text append 여부, 병합
당시 부모-자식 관계) 확인 필요.
- [x] **원인 확정(원격 DB 실측)**: 부모-자식 관계 없는 문서와 합쳐진 게 아니라, **onehop
      체인 자체가 정상 동작**했으나 `same_subject` 판정 기준이 과도하게 관대해서 벌어짐.
      `raw_inbox`(id 218~223) 확인: 사용자가 `memclaw.net/use-cases/memclaw-bestman`("we
      married..." 블로그 글)을 텔레그램으로 전송(2026-07-14 05:54) → onehop 자동확장이
      6분 뒤 같은 사이트/관련 사이트에서 5개 링크(`/docs`, `/docs/agents`, `/use-cases`,
      github repo, `caura.ai` 홈페이지)를 발견해 전부 `same_subject=true` 로 판정 → 새
      문서 대신 부모(`doc_d1f422458f69`)에 전부 흡수(raw_text append + `meta.extra_sources`).
      결과: 특정 유즈케이스 서사 글(블로그 포스트)의 `raw_text` 뒤에 제품 문서/유즈케이스
      목록/깃허브 README/회사 마케팅 홈페이지 본문이 통째로 붙어버림 — Gemini 요약이
      이 뒤섞인 본문 전체를 재추출하면서 "we married..." 글의 구체적 서사가 일반적인
      제품 소개로 희석/오염됨(사용자가 "엉뚱한 문서에 흡수됐다"고 느낀 원인).
      **추가로 발견한 2차 문제**: 흡수된 5개 중 github repo(`github.com/caura-ai/caura-memclaw`)는
      **이미 2026-06-12 에 사용자가 직접 보내 독립 문서로 존재**(`doc_0cba22ab679e`,
      raw_inbox#40) — `merge_source_into_document()`(`pipeline.py:350`)가 병합 전에 기존
      dedup(canonical_url/content_hash/minhash) 체크를 전혀 안 거쳐 **완전 동일 콘텐츠가
      두 문서에 중복 존재**하게 됨(하나는 독립 문서, 하나는 다른 문서 속 부속 섹션).
- [x] **판정 로직 코드 추적 완료**: `judge_research()` 의 `same_subject` 채점 기준
      (`gemini_provider.py:365-367`) = "[보고서]가 [맥락]이 다루는 대상 **그 자체**(공식
      저장소·공식 문서·공식 사이트 등 1차 출처)에 관한 내용이면 true". 설계 의도
      ([[docs/ONEHOP_MERGE_DESIGN.md]] §1)는 "GeekNews 2차 소개글 → 그 글이 다루는
      프로젝트의 github 원본 저장소(=사실상 같은 대상을 가리키는 두 창)" 케이스 한정이었는데,
      프롬프트 자체는 "같은 조직/제품의 공식 출처인가"만 보고 "부모 글이 다루는 구체적
      소재와 실질적으로 같은 내용인가"는 구분하지 않음. 이번 케이스처럼 **부모 글 자체가
      이미 1차 출처(회사 자체 블로그의 특정 유즈케이스 서사)**인 경우, 같은 회사의 다른
      공식 페이지(문서/다른 유즈케이스 목록/마케팅 홈페이지)는 전부 "공식 출처"라는 이유로
      true 오판정 — 설계 의도한 케이스(2차 소개글→원본)와 이번 사고 케이스(1차 소스→같은
      조직의 다른 1차 소스)를 프롬프트가 구분 못 함.

### 해결방안 제안 (구현 전 확인 필요)
1. **`same_subject` 프롬프트 수정**: "[맥락] 자체가 이미 공식 1차 출처(회사 자체
   블로그/제품 페이지 등)인 경우, 같은 조직·제품이라는 이유만으로 true 로 판정하지
   말 것 — [보고서]가 [맥락]이 다루는 **그 구체적 서사/소재**와 사실상 같은 것을
   가리킬 때만(예: 소개글이 다루는 프로젝트의 원본 저장소 자체) true, 문서/다른
   유즈케이스/마케팅 홈페이지처럼 다른 각도·다른 성격의 콘텐츠는 false" 로 기준 명확화 +
   good/bad 예시 추가.
2. **병합 전 기존 dedup 체크 추가**: `merge_source_into_document()` 호출 전(또는 그
   직전 게이트)에 child 의 `canonical_url`/`content_hash`/minhash 로 **이미 독립
   문서로 존재하는지** 확인 — 존재하면 병합하지 말고 스킵(또는 관계만 연결), 중복 흡수
   방지.
3. **`doc_d1f422458f69` 복구**: 정적 페이지라 원본 URL 재fetch 가능 → raw_text 를
   블로그 글 본문만으로 되돌리고 `meta.extra_sources` 5건 제거, `extract_resolve_store`
   재실행으로 엔티티/detail 재생성. github repo 부분은 이미 `doc_0cba22ab679e` 로 별도
   존재하므로 그쪽은 손댈 필요 없음(오히려 중복이 사라져 정리됨).
- [x] **구현+실증 검증 완료(로컬)**:
  1. `same_subject` 프롬프트 강화(`gemini_provider.py`): "2차 서술(제3자 작성) → 1차
     원본"으로 true 조건을 명확히 좁히고, "[맥락]을 쓴 주체가 조사 대상 본인(자사
     블로그 등)이면 같은 조직의 다른 공식 자료라도 형제 문서일 뿐 false" 규칙 + 예시 추가.
     **실제 Gemini API 호출로 검증**(mock 아님): 이번 사고의 실제 문서 6쌍(부모 raw_text +
     자식 5건, 사고 데이터 그대로) + 설계의도 긍정대조군 1건(pytorch.kr Hermes HUD 소개글
     → 공식 github, 실제 운영 DB 기존 정상 병합 사례) 로 테스트 → **6/6 정확 판정**
     (긍정대조군 true 유지, 사고 재현 5건 전부 false 로 정정). 스크립트/원본 데이터는
     `/tmp/.../scratchpad/verify_same_subject.py`(세션 한정, 재현 필요시 재작성).
  2. `canonicalize_url`(`normalize.py`) `_TRACKING` 에 `twclid` 추가 — 실측: 흡수된
     github repo 링크가 6/12 에 `?twclid=...` 파라미터 붙은 채로 독립 적재돼 canonical_url
     이 달라졌고, onehop 후보 필터(`_already_ingested`)가 못 거름. 회귀 테스트 추가
     (`test_pipeline.py::test_canonicalize_converges_equivalent_forms`, 실제 사고 URL의
     twclid 값 그대로 사용).
  3. `merge_source_into_document()`(`pipeline.py`) 병합 실행 전 dedup 체크 추가(content_hash
     → canonical_url → minhash 근사중복 순, `ingest()` 의 dedup①②③과 동일 우선순위) —
     canonicalize_url 이 못 거른 새로운 트래킹 파라미터가 또 나와도 2중 방어. 신규 테스트
     `test_onehop_merge.py::test_same_subject_child_already_exists_independently_skips_merge`
     로 검증(이미 독립 문서로 존재하는 콘텐츠는 병합 안 되고 스킵됨을 확인).
  - 로컬 `scripts/ci.sh`(uv.lock 동기 + 테스트) 192개 전부 통과.
- [x] **원격 배포 + 복구 완료**: `./deploy.sh`(CI 게이트 통과, 192개 테스트) → 6컨테이너
  재빌드·기동, `/health` degraded:false 확인. `IngestService.refresh_document(
  'doc_d1f422458f69', 'https://memclaw.net/use-cases/memclaw-bestman')` 로 원문 재fetch
  →in-place 갱신: raw_text 53407자→**7248자**(로컬에서 병합 마커 기준 분리해둔 원본
  길이와 정확히 일치 — 복구 정확성 교차검증), `meta.extra_sources` 완전 제거, detail
  (한국어 요약) 재생성 확인(더 이상 docs/마케팅 내용 안 섞이고 "we married" 서사에 집중된
  내용으로 정상화). 흡수됐던 github repo 는 원래 독립 문서(`doc_0cba22ab679e`)로 유일하게
  남아있음(중복 해소 확인, count=1). 작업 전 수동 백업 스냅샷(`claire-20260714-114045.db`)
  확보.
  **알려진 잔여 리스크(기존에 이미 수용된 패턴, 신규 아님)**: `extract_resolve_store` 는
  엔티티/관계를 삭제 없이 누적하는 구조라, 잘못 병합됐던 동안 그래프에 새로 생겼을 수 있는
  entities/relations(예: Caura.AI, "For Agents" 관련 개념)가 있다면 이번 복구로 doc 의
  raw_text/detail 은 정상화됐어도 그래프에 잔류할 수 있음 — `refresh_document()` 자체가
  안고 있는 기존 리스크와 동급([[docs/ONEHOP_MERGE_DESIGN.md]] §3.3 명시). 사용자가 그래프
  잔류 여부까지 확인을 원하면 별도 점검 필요(오늘 범위 밖, 요청 시 진행).

## 2. 숨기기 버튼 위치 — 목록이 아니라 상세 패널로
**증상**: 🙈 숨기기 버튼이 좌측 글 목록의 각 아이템에 붙어 나옴([[TODO_2026-07-13.md]] #7).
**요청**: 글 클릭 후 우측에 뜨는 상세 설명 패널 쪽으로 옮기고, 아이콘 버튼이 아니라
텍스트 버튼으로.
- [x] UI(`graphview.py`): `docItemHtml()`에서 🙈 버튼 제거(⭐즐겨찾기·📖읽기 2종만 남음).
      `document_detail()` 응답에 `hidden` 필드 추가, 상세 패널(`renderDocPanel`)에
      "숨기기"/"숨김 해제" 텍스트 버튼(`#panelhidebtn`, 링크 스타일 CSS) 추가.
      `toggleHide()`가 컨펌 취소 여부를 반환하도록 수정 + `panelToggleHide()` 신설로
      패널 버튼 라벨만 즉시 갱신(전체 패널 재로드 없이).
- [x] **Playwright 실증**: 목록에 🙈 없음 확인, 문서 클릭→패널에 "숨기기" 버튼 확인→
      클릭 시 컨펌 다이얼로그(문구 동일)→수락→목록에서 사라지고 "🙈 숨김 1개 보기"
      등장+패널 버튼이 "숨김 해제"로 즉시 갱신+DB `hidden=1` 반영 확인. "숨김 해제"
      클릭(컨펌 없음)→DB `hidden=0` 즉시 반영 확인.

## 3. 글 목록 제목/설명이 height를 너무 많이 차지함
**요청**: 목록에 문서가 많아지면 제목+설명이 세로 공간을 너무 잡아먹는다. 설명 줄수를
토글(0줄/2줄/4줄)로 조절 가능하게.
- [x] UI(`graphview.py`): `#docs .dhead`에 "설명 0/2/4줄" `<select>` 추가. CSS
      `#docs.lc0 .docitem p{display:none}` / `#docs.lc4 .docitem p{-webkit-line-clamp:4}`
      (기본 2줄)로 토글, `localStorage.claireDescLines`로 브라우저에 기억(기존
      `claireTheme`/`claireReadFS`와 동일 패턴).
- [x] **Playwright 실증**: 0줄(제목만)/2줄/4줄 전환 시 즉시 반영, 새로고침 없이 스크린샷으로
      각 단계 확인.

## 4. 즐겨찾기 목록에서 문서 높이 초과 시 overflow
**증상**: 즐겨찾기 섹션([[TODO_2026-07-13.md]] #7)에서 글 하나에 할당된 height보다 내용이
더 많아 밖으로 넘침. 일반 글 목록도 같은 문제가 있는지는 미확인.
- [x] **원인**: `.docitem b`(제목)에 line-clamp 가 전혀 없어 긴 제목이 무제한으로 늘어남
      (설명은 3번 토글로 제한되지만 제목은 그대로) → `.docitem{overflow:hidden}` +
      `.docitem b{-webkit-line-clamp:2}`(제목 2줄 고정, word-break 포함) 추가. 즐겨찾기·
      일반 목록이 `docItemHtml()`을 공유해 **양쪽에 동일 적용**(모듈화 — 즐겨찾기에서만
      확인된 증상이지만 일반 목록도 같은 렌더 함수라 근본 수정 시 함께 해결됨).
- [x] **Playwright 실증**: 제목/설명 모두 아주 긴 시드 문서로 즐겨찾기·일반 목록 양쪽 확인 —
      각 아이템이 자기 박스 안에 clamp 되어 다음 아이템을 침범하지 않음.

## 5. 웹 적재 완료 후 "문서보기" 링크 클릭 시 무한 로딩
**증상**: 웹에서 문서 적재 요청 → 완료 시 설명 패널에 "문서보기" 링크 표시 → 클릭하면
"불러오는 중"으로 바뀐 채 아무 반응 없음. 버그로 추정.
- [x] **원인 확정**: `loadDocPanel(id)`는 `if(activeDoc!==id) return;` 가드로 그 사이
      다른 문서로 이동했으면 렌더를 스킵한다(정상 설계 — 빠른 연속 클릭 시 경쟁 상태
      방지). 그런데 적재 완료 후 "문서 보기" 링크는 `loadDocPanel(d.document_id)`를
      **직접** 호출해 `activeDoc`을 세팅하는 과정을 건너뛰었음 → `activeDoc`이 그
      문서 id 와 항상 달라 가드에 걸려 fetch 는 성공해도 렌더가 영원히 스킵됨(그래서
      "불러오는 중"에 멈춘 것처럼 보임). 좌측 목록 클릭은 `selectDoc()`을 거쳐
      `activeDoc`을 먼저 세팅하므로 정상 동작했던 것과 대비.
- [x] **수정**: 링크의 `onclick`을 `loadDocPanel(...)` → `selectDoc(...)`로 변경(좌측
      목록 클릭과 동일 경로 재사용 — 그래프 강조·활성 표시도 덤으로 따라옴).
- [x] **Playwright 실증**: 메모 텍스트 적재→완료 화면의 "문서 보기 →" 클릭→우측 패널이
      즉시 그 문서 상세(제목/요약/노드)로 전환됨을 확인(더 이상 멈춤 없음).

---
## 진행 로그
- 2026-07-14: 사용자 제보 5건 접수, 문서화.
- 2026-07-14(계속): 1번(문서 오병합) 원인 확정(원격 DB 실측)·수정 3건(same_subject
  프롬프트, twclid 트래킹 파라미터, 병합 전 dedup 체크)·실제 Gemini API 검증(6/6)·로컬
  CI 192개 통과·원격 배포·`doc_d1f422458f69` 복구 완료.
- 2026-07-14(3차): 2~5번 UI 수정 — 로컬 mock provider + 시드 데이터로 `serve-api` 띄우고
  Playwright 로 4건 전부 실제 브라우저에서 동작 확인(숨기기 텍스트버튼 이동, 설명
  0/2/4줄 토글, 제목/설명 clamp 로 overflow 방지, 문서보기 링크 버그 수정). JS 문법
  `node --check` 통과, 로컬 `scripts/ci.sh` 192개 재확인. 원격 배포 완료, `/health`
  degraded:false 확인. **오늘 제보 5건 전부 조사·수정·검증·배포 완료.**

**다음 세션 이어받을 것**: 없음(오늘 제보 5건 전부 완료·배포됨). 단, 1번 항목에서
언급한 잔여 리스크 — 오병합 기간 중 그래프에 쌓였을 수 있는 엔티티/관계(Caura.AI,
"For Agents" 등)가 정리 안 된 채 남아있을 가능성은 열려있음(사용자가 확인 원하면
`doc_d1f422458f69` 기준으로 점검). 그 외 열려있는 항목은 [[TODO_2026-07-13.md]] #5
(MCP 노출, 후순위)뿐.
