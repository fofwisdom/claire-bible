# TODO 2026-08-09 — 텔레그램 자동복구 영구실패(inbox#276) 조사

원격 텔레그램 로그에서 자동복구 영구실패 알림 발견. 작업 협약(CLAUDE.md)에 따라
증상 → 원인 → 수정 → 검증 순으로 기록.

## 1. inbox#276 fetch 영구실패: `Invalid non-printable ASCII character in URL, '\n' at position 65.`

**증상(원문, 텔레그램 알림)**:
```
[2026-08-09 오전 11:05] 조경일: https://paper.blackan.net/export/201e0a9b77e43fe82ab925148fe53034
TRAJDEBUG: Tracing Error Lifecycle to Identify Critical Failures in Long-Horizon Agent Trajectories
[2026-08-09 오후 12:28] blackan_claire_bible_bot: ⛔ claire 자동복구 영구실패 1건 (재시도 상한 도달)
• inbox#276: fetch failed: Invalid non-printable ASCII character in URL, '\n' at position 65.
```

- [x] **원인 확정**: `src/claire/ingest/router.py`의 `fetch()`/`classify()`는
      「제목 + 트레일링 URL」(제목이 먼저, 끝에 링크) 공유 패턴만
      `extract_shared_url()`로 걸러냈다. 이번 메시지는 **반대 패턴** —
      링크가 먼저 오고 그 아래 줄에 제목/설명이 붙는 형태
      (`https://.../201e...\nTRAJDEBUG: ...`). 이 경우
      `t.lower().startswith(("http://","https://"))`가 `True`라 그
      분기를 타지 않고, 텍스트 **전체**(URL + 개행 + 제목 문장)를 그대로
      URL로 오인해 `fetch_web()` → `httpx`에 넘김. `httpx`가 URL 안의
      개행(`\n`, 65번째 위치 — 정확히 순수 URL 길이와 일치)을 비인쇄
      ASCII 문자로 거부하며 예외 발생. `data/raw` 원본은 보존돼 있어
      재현·검증 가능했음(삭제 없음).
- [x] **수정**: `router.py`에 `leading_url()` 헬퍼 추가 — 텍스트가
      `http(s)://`로 시작하면 첫 공백(개행 포함)/괄호/따옴표 전까지만
      URL로 자르고 뒤따르는 캡션은 버린다(`extract_shared_url()`과
      대칭되는 역방향 패턴 처리). `fetch()`와 `classify()`의 host 판별
      양쪽에 적용. `telegram_bot.classify_input()`(사용자 안내 라벨링)도
      동일 규칙으로 맞춤 — 기존 주석대로 "router 와 동일 규칙" 유지.
- [x] **검증**: `tests/test_router.py`에 inbox#276 재현 케이스 포함 회귀
      테스트 5건 추가(`leading_url` 단위 테스트, 캡션에 다른 서비스명이
      섞여도 실제 URL 호스트로 분류되는지, `fetch()`에 전달되는 최종 url에
      개행이 없는지, `classify_input` 라벨 일치). `pytest` 전체
      261 passed, 6 subtests passed(회귀 없음).
- [x] **후속**: 이 세션에서 항목 2/3/4/5 작업 중 `deploy.sh`를 두 차례
      실행하면서(작업 트리 전체를 rsync하는 방식이라) 이미 커밋돼 있던
      이 수정도 함께 원격에 반영됨. 원격 DB 확인 결과 `raw_inbox#276`은
      `status=done, error=(없음)`으로 이미 성공 처리됨(자동복구 루프가
      배포 후 자체 재시도한 것으로 보임, 별도 수동 `/retry` 불필요).

## 2. 문서 타이틀 오적재 + 자동 업데이트가 실패 메시지로 원본 제목을 덮어씀

**증상(사용자 구두 보고, 원인 파악 전)**: 원격에 저장된 문서들을 읽다가 발견.

- [x] **원인 조사(1차, Explore agent)**:
  - **2-1. "Writing — Armalo" (자동 덮어쓰기, 가장 유력/심각)**:
    `src/claire/ingest/service.py` `refresh_document()`(L202-281)이
    주기 재조회(`claire_refresh`/`claire_recover` 컨테이너) 시,
    fetch가 예외 없이 "성공"만 하면 `content_hash`가 달라졌다는 이유만으로
    L260-263에서 `update_document_content()`를 호출해 title/raw_text를
    **무조건 덮어씀**. 새로 받아온 콘텐츠가 봇차단/삭제/로그인게이트
    페이지처럼 품질이 나빠도(길이만 충족하면) 걸러내는 가드가 없음.
    `old_len`을 계산해두고도 `new_len`과 비교하는 로직이 없음(L221 대비
    L255-263). `watch_enabled==1`인 문서만 `save_document_snapshot()`으로
    옛 원문이 스냅샷에 남고(L248-254), `watch_enabled==0`이면 스냅샷도
    없어 원본이 완전 유실됨. `db.py` `update_document_content()`(L1168-1193)
    쪽도 호출자 신뢰 기반이라 자체 가드 없음.
  - **2-3. "Just a moment..." (Cloudflare 인터스티셜 오인, 유력)**:
    `src/claire/ingest/fetchers/web.py` `fetch_web()`(L46-100)의
    static→discourse→scrapling→cdp 에스컬레이션이 **오직 텍스트 길이
    (`MIN_CONTENT=300`)**로만 "성공"/"더 나은 결과"를 판단(L56-75).
    Cloudflare/봇차단 인터스티셜, 로그인 게이트 등을 감지하는 로직이
    코드베이스 어디에도 없음(grep 확인). `_fetch_cdp()`(L283-339)가
    렌더링한 "Just a moment..." 페이지가 이전 단계보다 길면 그대로
    정상 콘텐츠로 채택되어 title로 저장됨.
  - **2-2. "Paper Insight" (og:title 우선순위, 부수적)**:
    `_extract_html()`(web.py L157-169)이 `og:title` > `<title>` > `<h1>`
    순으로 제목을 채택. `og:title`이 사이트 전역 브랜드명으로 고정된
    SPA(사용자 자신의 서비스)에서는 실제 콘텐츠 제목보다 브랜드명이
    우선 채택됨.
  - **추가 확인 필요**: 실제 피해 문서 3건의 `fetch_via` 메타값과
    `watch_enabled` 여부를 DB에서 직접 조회해 가설을 확정할 것
    (복구 가능 여부도 이 값에 좌우됨 — 스냅샷이 있으면 원문 복구 가능).
- [x] **원인 확정(2차, 원격 DB 직접 조회)**:
  - 3건 모두 `meta.fetch_via = "cdp"` — 가설 2(인터스티셜 미감지) 뒷받침.
  - `doc_86b78d61076d`(Armalo) `watch_enabled=1`. `document_snapshots`에
    2026-08-07 11:47:39 캡처분 존재: 원제목
    "Hermes Agent Benchmark: The Complete Guide (2025–2026)", raw_text
    20000자. 반면 현재 `documents.raw_text`는 722자 "404 · Page not
    found ... This page is not in the workspace ..." — **가설 1(무조건
    덮어쓰기) 완전 확정**. 원본은 스냅샷에 살아있어 삭제되지 않았음
    (데이터 보존 원칙 위반 아님, 다만 메인 레코드가 오염된 상태).
  - `doc_9d3805bfbf1c`("Just a moment...")·`doc_0ab12043deab`("Paper
    Insight")는 `watch_enabled=0`이라 스냅샷 없음 — 이 둘은 애초
    최초 수집 시점부터 나쁜 콘텐츠였던 것으로 판단(덮어쓰기가 아니라
    최초 fetch 품질 문제).
  - 영향 범위 추정: `watch_enabled=1`인 문서 중 raw_text에 알려진
    실패/인터스티셜 문구가 포함된 것은 현재 이 1건뿐(전수 아님 — 이
    패턴 문구에 안 걸리는 다른 형태의 오염은 놓쳤을 수 있음, 추가
    피드백 시 재확인).
- [x] **수정(로컬 코드, 커밋 전)**:
  1. `fetch_web()`(`web.py`)에 `_looks_like_failure_page()`/`_content_score()`
     추가 — Cloudflare "Just a moment" 류, "404"/"page not found"/
     "not in the workspace" 류의 알려진 실패 패턴이 본문 앞부분(400자)에
     감지되면 그 단계 결과를 "성공"으로 채택하지 않고 다음 fallback으로
     에스컬레이션. 체인 끝까지 실패 패턴만 남으면 `FetchError`로 명시
     실패 처리(길이 기준만으로 통과되던 기존 thin-guard 를 보강).
  2. `refresh_document()`(`service.py`)에 품질 가드 추가 — `old_len>=1000`
     이고 `new_len < old_len*0.3`이면(대폭 축소) `status="held_low_quality"`
     로 반환하고 기존 title/raw_text는 **건드리지 않음**(2중 방어). 이
     상태는 `run_refresh_queue`에서 기존 로직 그대로 `error`로 기록돼
     텔레그램 `/failed`·`/retry` 알림 대상이 됨(추가 배선 불필요).
  3. og:title 우선순위 문제(2-2, "Paper Insight")는 **이번 수정에 미포함**
     — 부수적 원인으로 판단해 범위에서 제외(추후 필요시 별도 처리).
- [x] **검증**: `tests/test_web_scrape.py`에 인터스티셜 감지 회귀 6건,
     `tests/test_refresh.py`에 품질가드 회귀 2건(Armalo 실사례 값 그대로
     재현하는 케이스 포함) 추가. `pytest` 전체 269 passed, 6 subtests
     passed(회귀 없음, 기존 261 passed 대비 +8건).
- [x] **원격 배포**: 사용자 확인 후 `./deploy.sh` 실행 — CI 게이트(uv.lock
     일관성 + pytest 269 passed) 통과, 6개 컨테이너(bot/api/refresh/
     recover/backup/expand) 재빌드+재기동 완료.
- [x] **`doc_86b78d61076d`(Armalo) 원격 데이터 복구**: 사용자 확인 후
     `claire_api` 컨테이너 안에서 `dbm.update_document_content()`로
     스냅샷 원본을 되돌림 — title/raw_text/content_hash 확인:
     복구 전 `"Writing — Armalo"`/722자/hash `b952cbcd…` →
     복구 후 `"Hermes Agent Benchmark: The Complete Guide
     (2025–2026)"`/20000자/hash `37561581…`(스냅샷과 일치). 오염된
     meta(404 페이지의 links/link_anchors/images)는 제거하고
     `restore_note`를 meta 에 남김. `ensure_document_detail(force=True)`
     로 한국어 가독 렌더링(detail, 2828자)도 새 본문 기준으로 재생성
     완료. 사용한 임시 스크립트는 컨테이너/원격 모두에서 삭제(정리).
  - 2-2("Paper Insight" og:title 우선순위)는 이번 스코프 밖 — 필요시
    후속 항목으로 별도 처리.

## 3. [UI/데스크탑] 즐겨찾기-문서 목록 사이 간격 조절 기능 요청

- [x] 데스크탑 버전에서 즐겨찾기 영역과 문서 목록 사이 gap을 기본값으로
      더 넓히고, 사용자가 마우스로 드래그해 gap 크기를 조절할 수 있게
      해달라는 기능 요청(신규 기능, 버그 아님).
- [x] **구현**(`src/claire/graphview.py`): `#pinnedlist`/`#doclist` 사이
      2px 테두리를 `#pinnedResizer`(10px, `cursor:row-resize`, 가운데
      grip 표시, hover/드래그 시 강조) 로 교체 — 기본 gap 자체가 넓어짐.
      드래그하면 `#pinnedlist`의 높이(CSS 커스텀 프로퍼티 `--pinned-h`)를
      40px~(사이드바 높이-120px) 범위로 조절, `localStorage
      ('clairePinnedH')`에 기억해 재방문 시 유지. 데스크톱 전용 —
      모바일(`@media max-width:820px`)은 즐찾이 탭 전환 방식이라
      `#pinnedResizer`를 그 미디어쿼리에서 숨김.
  - **구현 중 발견한 버그(수정)**: 처음엔 JS 가 `#pinnedlist.style.maxHeight`
    에 인라인 px 를 직접 썼는데, 인라인 스타일은 특이도상 어떤 클래스
    셀렉터보다도 항상 이겨서 모바일 탭 모드의 `max-height:none` 규칙을
    깨버림(Playwright 로 뷰포트 전환 테스트 중 발견). CSS 커스텀
    프로퍼티(`--pinned-h`)를 거치는 방식으로 바꿔 캐스케이드가 뷰포트별
    분기를 정상적으로 처리하게 수정.
- [x] **검증**: `pytest tests/test_graphview.py` 12 passed(회귀 없음).
      로컬에서 `claire serve-api`(테스트 DB) 기동 후 Playwright headless
      Chromium 으로 실제 브라우저 조작 검증 — 즐찾 2건 pin, 리사이저
      드래그로 축소(184px→44px)/확대, `localStorage` 영속 후 새로고침
      유지 확인, 뷰포트를 480px 로 줄여 모바일 탭 모드에서
      `max-height:none`이 정상 적용되는지(수정 전엔 실패, 수정 후 통과)
      확인. 테스트용 pin은 원복, 로컬 서버는 종료.

## 4. [UI] 좌측 문서 목록에 '읽지 않음' 필터 추가

- [x] 좌측 문서 목록에 '읽지 않음' 필터를 추가해달라는 요청. 항목의 설명
      2줄 영역 쪽 width에 여유가 있어 보이니, 그 공간을 필터 UI와
      나눠 쓰는 배치를 제안(사용자 의견, 신규 기능, 버그 아님).
- [x] **구현**(`src/claire/graphview.py`): 기존 "설명 줄수" `<select>`를
      단독 줄에서 flex 행으로 바꿔(`flex:1`) 같은 행 오른쪽에 "읽지 않음"
      체크박스(`#unreadFilter`)를 배치(요청대로 여유 width를 나눠 씀).
      `renderDocs()`의 `match()` 필터에 `dc.seen===0` 조건을 추가해 체크
      시 즐겨찾기·일반 목록 양쪽 모두 미열람 문서만 남긴다. 다른 토글들과
      동일 패턴으로 `localStorage('claireUnreadOnly')`에 상태 기억.
- [x] **검증**: `pytest tests/test_graphview.py` 12 passed. 로컬
      serve-api(테스트 DB, 미열람 3·열람 2건 혼합)에 대해 Playwright로
      확인 — 기본 5건 노출 → 필터 체크 시 3건(전부 `.unread` 클래스)만
      노출 → 새로고침해도 체크 상태·필터링 유지 → 해제 시 5건 복원.

## 5. [UI] 좌측 문서 목록 '크게 읽기' 아이콘 배경색 대비 부족

- [x] 좌측 문서 목록의 '크게 읽기' 아이콘 배경이 회색이라 책 모양 이모지가
      배경과 뚜렷이 구분되지 않음. '크게읽기' 큰 버튼 버전처럼 배경을
      파란색으로 통일해달라는 요청(UI 버그/일관성).
- [x] **원인**: 데스크톱의 📖 버튼은 `class="actbtn readbtn"` 두 클래스를
      함께 들고 있는데(`docItemHtml`), `.actbtn`(회색, 즐찾 버튼과 공유하는
      기본 배경)이 배경을 차지해 회색으로 보였음 — 모바일 리빌 배지 버전은
      별도 `@media` 규칙에서 `background:var(--accent)`(파란색)로 완전히
      재정의돼 있어 그쪽만 파란색이었음.
- [x] **수정**: `src/claire/graphview.py` — 처음엔 `.docitem .readbtn{...}`
      단일 클래스 규칙에 배경을 얹었으나, `.actbtn`이 소스상 더 뒤에 있어
      같은 특이도에서 다시 덮어써지는 걸 검증 중 발견(재현: 배경이 여전히
      회색). 소스 순서에 기대지 않도록 복합 셀렉터
      `.docitem .actbtn.readbtn{background:var(--accent);
      border-color:var(--accent);color:#fff}`로 특이도를 올려 확실히
      이기게 수정 — 모바일 리빌 배지와 동일한 `--accent` 파란색으로 통일.
- [x] **검증**: `pytest tests/test_graphview.py` 12 passed. Playwright로
      실제 컴퓨티드 스타일 확인 — 라이트 테마 데스크톱 `rgb(9,105,218)`
      (`#0969da`=`--accent`), 다크 테마 `rgb(88,166,255)`(`#58a6ff`),
      모바일 뷰포트(480px)에서도 동일 파란 배경 유지(회귀 없음, border는
      모바일 쪽 `border:0`으로 시각적 영향 없음) 확인.

## 세션 마무리 (핸드오프)

- **상태**: 1~5번 항목 전부 원인 확정→수정→검증→원격 배포까지 완료. 원격
  6개 컨테이너(bot/api/refresh/recover/backup/expand) 정상 기동 확인,
  `pytest` 전체 269 passed·6 subtests passed(로컬 기준, 회귀 없음).
- **커밋 대상**: `src/claire/graphview.py`(3·4·5번 UI),
  `src/claire/ingest/fetchers/web.py`·`src/claire/ingest/service.py`(2번
  버그 수정), `tests/test_refresh.py`·`tests/test_web_scrape.py`(회귀
  테스트), 이 `TODO_2026-08-09.md`. 배포는 rsync 기반(`deploy.sh`)이라
  커밋 여부와 무관하게 이미 원격에 반영되어 있었음 — 이번 커밋은 그
  워킹트리 상태를 이력으로 남기는 것.
- **의도적으로 남겨둔 것**: 2-2("Paper Insight" og:title 우선순위 —
  og:title이 사이트 브랜드명으로 고정된 SPA에서 실제 콘텐츠 제목 대신
  브랜드명이 채택되는 문제)는 부수적 원인으로 판단해 이번 스코프에서
  제외함. 재발/추가 사례 나오면 `web.py` `_extract_html()`의 title 우선순위
  로직(og:title > title > h1)을 다시 볼 것.
- **다음 세션에서 참고할 점**: 로컬에서 `claire serve-api` + Playwright로
  UI를 실제 브라우저 조작까지 검증하는 방법을 이번에 확립함(세션 토큰은
  `dbm.create_session(conn, scope='owner')`으로 직접 발급, `?t=<token>`
  진입 → 쿠키 저장 → 이후 요청에 재사용). `serve-api`는 sandbox가
  SIGTERM으로 죽이므로 `run_in_background:true` + `dangerouslyDisableSandbox:true`
  둘 다 필요(CLAUDE.md에 이미 기록됨). Playwright는 CDN 리소스(외부
  네트워크) 요청 시 헤드리스 브라우저가 간헐적으로 멈추는 문제가 있어
  `page.route('**/*', ...)`로 localhost 외 요청을 차단하면 안정적.
