# Claire Bible — 목표와 로드맵

## 1. 비전과 범위

Claire는 링크·문서·메모를 수집해 구조화하고, 기존 지식과 연결하여 검색과 종합에
활용하는 단일 사용자용 지식베이스다.

| 범위 안 | 범위 밖 |
|---|---|
| 수집·정규화·중복 제거 | 멀티테넌시 |
| 구조화 추출·엔티티 해소 | 결제·구독·온보딩 |
| 그래프·키워드·벡터 검색 | 수평 확장 |
| 컨테이너 설치·업데이트·상태 점검 | 공개 인터넷에 무보호로 노출 |

## 2. 현재 제공 기능

- 텔레그램, CLI, 로컬 API가 같은 적재 파이프라인을 사용한다.
- 일반 웹, 리다이렉트, YouTube, X 게시물, 텍스트와 파일을 정규화된 문서로 변환한다.
- canonical URL과 콘텐츠 해시를 이용해 중복을 제거하고 갱신을 처리한다.
- Gemini 또는 mock provider로 엔티티·관계·요약을 구조화한다.
- SQLite를 정본으로 사용하고 FTS·벡터·그래프 관계를 결합해 검색한다.
- 그래프 웹 UI, 문서 상세 보기, 검색, 종합 문서 생성과 공유 링크를 제공한다.
- 자동 재시도, 주기 갱신, 1홉 확장, 상태 점검과 운영 알림을 제공한다.
- `cb-manuscript`로 운영·개발 컨테이너 설치와 업데이트를 일관되게 수행한다.

## 3. 품질 원칙

- 원문과 출처를 보존하고 생성 결과만으로 원본을 대체하지 않는다.
- 실패한 입력은 복구 가능한 상태로 기록하고 무한 재시도하지 않는다.
- 결정론적 로직은 단위 테스트로, 모델 의존 로직은 별도 평가 표본으로 검증한다.
- 읽기와 쓰기 권한을 분리하고 외부 노출 전 네트워크·애플리케이션 인증을 확인한다.
- 설정과 자격증명은 환경변수로만 주입하며 저장소에 운영 데이터를 포함하지 않는다.

## 4. 로드맵

### 안정성

- [x] 오류 inbox와 지수 백오프 기반 자동 복구
- [x] 헬스·상태·통계 명령
- [x] 통합 컨테이너 설치·업데이트 진입점
- [ ] 백업·복구 별도 재설계
- [ ] 장애 주입 기반 복구 평가 확대

### 추출과 연결 품질

- [x] canonical URL과 콘텐츠 해시 기반 중복 처리
- [x] 약어·별칭 기반 엔티티 해소
- [x] 1홉 자료 확장과 같은 주제 병합
- [ ] 라벨된 must-merge/must-not-merge 평가셋 확대
- [ ] 정적 웹 페이지의 boilerplate 제거 품질 개선

### 검색과 UI

- [x] FTS와 벡터를 결합한 하이브리드 검색
- [x] 그래프 탐색·필터·관계 경로 UI
- [x] 다중 노드 기반 종합 문서
- [x] 읽기 전용 세션과 문서 단위 공유 링크
- [ ] 종합 결과의 재흡수·계보 관리 개선

## 5. 공개 저장소 데이터 정책

- `sample.md`에는 공개 문서와 합성 메모만 사용한다.
- 재생 로그, 데이터베이스, vault, 조사 결과와 실제 운영 설정은 버전 관리하지 않는다.
- 문서의 주소·계정·포트는 예시값 또는 환경변수 이름으로만 표기한다.
- 이슈와 테스트 fixture에는 실제 사용자 메시지나 운영 DB 식별자를 복사하지 않는다.
