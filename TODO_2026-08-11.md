# TODO 2026-08-11 — UI 피드백 4건

사용자가 구두로 전달한 개선 요구사항. 작업 협약(CLAUDE.md)에 따라 먼저 항목별로
정리한 뒤 하나씩 처리한다.

## 1. [UI/그래프] 노드가 많아지니 그래프가 너무 조밀해서 보기 불편함

**증상(원문)**: "그래프 노드가 많아지니, 현재 너무 조밀하게 있어서 보기 불편함."

- [x] **원인(코드 확인)**: `src/claire/graphview.py:1244` `new vis.Network` 옵션 —
      `physics:{barnesHut:{gravitationalConstant:-8000, springLength:120}}`.
      노드 수가 늘어도 반발력/스프링 길이가 고정값이라 안정화 후 노드 간 평균
      간격이 좁아짐.
- [x] **수정**: `gravitationalConstant -8000→-12000`, `springLength 120→170`으로
      상향(반발력·기본 간격 확대). 안정화 iteration 수(200)는 그대로 유지.
- [x] **검증(합성 그래프, 로컬 Playwright)**: 실 DB는 문서 5건뿐이라 밀집도를
      볼 수 없어, 이전 세션 벤치마크 규모(605노드/659엣지)에 맞춰 600노드/646엣지
      합성 그래프를 만들어 확인. (1) 안정화 후 모든 노드 좌표가 finite(freeze/NaN
      없음) 확인. (2) 같은 그래프에 기존값/신규값을 각각 적용해 bbox 비교 —
      기존(−8000/120) 6024×5946 → 신규(−12000/170) 7413×7294로 노드 분포 영역이
      약 23~25% 넓어짐(정량 확인, 수치는 재현 가능).
- [ ] **남은 것**: 이건 튜닝이라 "보기 편한 정도"는 결국 실제 그래프(600+ 노드)를
      직접 봐야 판단 가능 — 배포 후 사용자가 눈으로 보고 부족/과함 피드백 주면
      추가 조정.

## 2. [UI/문서목록] 즐겨찾기 시 본래 문서 목록에서 사라지는 문제

**증상(원문)**: "글에 즐찾 시 본래 기사 목록에서 지우지 말것."

- [x] **원인(코드 확인)**: `graphview.py` `renderDocs()`(약 1770행)에서
      `rest = visible.filter(dc=>dc.pinned!==1)` — 즐겨찾기(pinned) 문서는
      상단 즐겨찾기 섹션에만 남고 일반 날짜별 목록(`#doclist`)에서는
      의도적으로 제외되고 있었음(설계된 동작, 버그 아님).
- [x] **수정**: `rest = visible`로 변경 — 즐겨찾기 문서도 원래 날짜별 목록에 그대로
      남고, 즐겨찾기 섹션에는 계속 중복 표시.
- [x] **검증(로컬 Playwright)**: `togglePin()`으로 문서 1건 즐겨찾기 지정 →
      `#pinnedlist`·`#doclist` 양쪽에 모두 나타남 확인, 목록 쪽 사본을 클릭해도
      `selectDoc()`이 에러 없이 정상 동작(같은 id 의 두 사본이 DOM에 있어도 문제
      없음 — 각 아이템은 자체 완결형 HTML 조각이라 `id` 속성 충돌 없음).

## 3. [UI/중복정리] 중복정리 창이 안 닫힘 — 기능 자체가 필요한지도 확인 필요

**증상(원문)**: "중복정리 창안닫힘. 실제 우리가 쓰는 기능인가? 없어도 되는
기능일지도?"

- [x] **원인 확정(2건 겹침)**:
  1. 모바일에서 패널(우측 `#panel`)을 여는 모든 경로(`loadNode` L1351,
     `selectDoc` L1884, `openPeekPanel` L1916)는 `openMobilePanelUI()`를
     호출해 뒤로가기 스택(`uiStack`)에 `'panel'`을 쌓는다. `openDedup()`
     (툴바 "♻️ 중복정리" 버튼에서 직접 호출)은 이 호출이 아예 빠져 있었음.
  2. 목록을 펼친 뒤 문서 하나를 revealed(peek) 상태로 만들어 놓은 채로
     열면, `openMobilePanelUI()`의 "이미 열려있으면 중복 push 안 함" 가드에
     걸려 (1)을 고쳐도 여전히 push가 안 되고, ✕ 버튼(`closePanelOrPeek`)은
     `revealedDocId`가 남아있는 걸 보고 "그 문서로 되돌아가는 것"으로 오인해
     peek 상태로만 되돌리고 완전히 안 닫힘.
- [x] **처리 방향(사용자 확인)**: 기능은 계속 쓰되(자동 정확 일치 중복은 이미
      ingest 단계에서 자동 처리 중 — 이 기능은 URL이 다른 "근사 중복"만 다룸,
      직접 유지문서 선택 기능은 이미 구현돼 있었음), 병합은 이미 확인창(`confirm()`,
      병합 전 자동 백업 안내)이 있어 그대로 유지 — 닫힘 버그만 수정.
- [x] **수정**: `openDedup()`에서 `revealedDocId=null` + `peek` 클래스 제거 후
      `openMobilePanelUI()` 호출 — dedup을 "리빌 문서와 무관한 새 패널"로
      명확히 취급.
- [x] **검증(로컬 Playwright)**: (1) idle 상태 → ♻️ 열기 → ✕ → 정상 닫힘.
      (2) 목록 펼침 → 문서 revealed(peek, `uiStack=['list','panel']`) →
      ♻️ 열기(스택 중복 push 없이 재사용 확인) → ✕ → `uiStack=['list']`,
      `panel.innerHTML`이 기본 힌트로 복원, `listopen` 유지 확인(두 경로
      모두 통과).

## 4. [UI/모바일] 목록+크게읽기 모드에서 뒤로가기 시 둘 다 한번에 닫힘

**증상(원문)**: "폰에 목록+크게 모드에서 뒤로가기하니 둘다 한번에닫힘."

- [x] **코드 검토**: 2026-07-24 세션에 유사 증상(목록+**우측 패널**(peek/reading))을
      이미 수정한 이력 있음(`TODO_2026-07-24.md` 1번). "크게읽기"(`#reader`
      팝업, `openReader()`)는 `pushUIState('reader')`를 정상 호출해 다른
      스택 경로(L960)로 분리돼 있음.
- [x] **재현 시도(로컬 Playwright, 모바일 뷰포트)**: 두 가지 흐름 모두 시도 —
      (a) 목록 펼침(1탭) → 크게읽기 버튼 직접 탭 → `uiStack=['list','reader']`
      → 뒤로가기 1번 → `reader`만 닫히고 `listopen` 유지(정상).
      (b) 목록 펼침(1탭) → 같은 아이템 재탭(revealed/peek, `uiStack=['list','panel']`)
      → 크게읽기 탭 → `uiStack=['list','panel','reader']` → 뒤로가기 1번 →
      `reader`만 닫히고 나머지 유지(정상).
      **두 경로 모두 재현 실패** — 현재 코드로는 뒤로가기 1번에 크게읽기만
      닫히고 목록은 유지됨.
- [ ] **다음 단계(사용자 확인 필요)**: 코드 리뷰+자동화 재현으로는 문제를
      못 찾음. 재현 조건을 더 구체적으로 알아야 함 — (1) "크게 모드"가 정확히
      📖 크게읽기 팝업(`#reader`)이 맞는지, 아니면 우측 패널이 전체 오픈된
      상태(`body.reading`)를 말하는 건지, (2) 실기기(어떤 브라우저/OS)의
      물리 뒤로가기 버튼인지 화면 엣지 스와이프 제스처인지, (3) 목록을 편
      순서와 크게읽기를 연 순서(정확한 탭 시퀀스). 이미 고쳐졌던 증상을
      기억으로 다시 말씀하신 걸 수도 있음 — 확인 후 처리.

## 진행 상태

- 1·2·3번: 원인 확정 → 수정 → 로컬 Playwright 검증 → 원격 배포까지 완료.
  `pytest` 전체 269 passed·6 subtests(회귀 없음, 단 이 3건은 전부 JS 라
  pytest로는 검증 안 됨 — 실제 검증은 Playwright).
- 4번: 코드 검토 + 두 가지 그럴듯한 재현 경로 모두 시도했으나 재현 실패.
  사용자 확인 결과 "일단 넘어가자"(2026-08-11) — 보류. 나중에 실제로 다시
  겪으면 그때 재조사(어떤 화면/버튼 조합인지, 실기기 뒤로가기 버튼인지
  스와이프 제스처인지 기억나는 대로 알려주면 재현에 도움).
- **배포**: 사용자 확인 후 `./deploy.sh` 실행 — CI 게이트(uv.lock 일관성 +
  pytest 269 passed) 통과, 6개 컨테이너(bot/api/refresh/recover/backup/expand)
  재빌드+재기동 완료(2026-08-11).

## 세션 마무리 (핸드오프)

- **상태**: 1~3번 원인 확정→수정→로컬 브라우저 검증→원격 배포까지 완료.
  4번은 재현 실패 후 사용자 확인으로 보류.
- **커밋 대상**: `src/claire/graphview.py`(1~3번 UI/버그 수정), 이
  `TODO_2026-08-11.md`. 배포는 rsync 기반(`deploy.sh`)이라 커밋 여부와
  무관하게 이미 원격에 반영되어 있음 — 이번 커밋은 그 워킹트리 상태를
  이력으로 남기는 것(2026-08-09 세션과 동일 패턴).
- **검증 방법(재사용 가능)**: 로컬 `data/claire.db`는 문서 5건뿐이라 UI
  밀집도 등 규모가 필요한 검증엔 못 씀 — 이번엔 스크래치패드에 별도
  테스트 DB(`setup_test_db.py`)를 만들고, 그래프 밀도 검증에는 600노드/
  646엣지 합성 그래프(`gen_big_graph.py`)를 추가로 얹어 확인했다(실 DB는
  건드리지 않음, 임시 DB는 세션 종료 시 삭제). `dbm.create_session(conn,
  scope='owner')`로 토큰 발급 → `?t=<token>` 접속 → 쿠키 재사용은
  2026-08-09 세션에서 확립한 방식 그대로. `CLAIRE_DB_PATH`/
  `CLAIRE_INJECT_PORT` 환경변수로 테스트 DB·포트 지정(기본 포트와 충돌
  방지). `serve-api`는 sandbox가 SIGTERM으로 죽이므로
  `run_in_background:true`+`dangerouslyDisableSandbox:true` 둘 다 필요
  (CLAUDE.md에 이미 기록됨).
- **다음 세션에서 참고할 점**:
  - 4번(목록+크게읽기 뒤로가기)은 재현 조건 확인 대기 중. 사용자가 다시
    겪으면 (1) 정확히 어떤 화면 조합이었는지("크게읽기 팝업" vs "우측
    패널 전체오픈"), (2) 실기기 뒤로가기 버튼인지 스와이프 제스처인지,
    (3) 목록을 편 순서와 크게읽기를 연 정확한 탭 시퀀스를 물어볼 것.
  - 3번(중복정리) 버그는 겉보기엔 단순해 보였지만 실제로는
    `openMobilePanelUI()`의 "이미 열려있으면 중복 push 안 함" 가드와
    `closePanelOrPeek()`의 `revealedDocId` 분기가 맞물려 두 단계였다 —
    이런 모바일 UI 상태 버그는 단일 경로만 재현하지 말고 "패널이 이미
    다른 상태(peek/reading)로 열려있는 채로 진입"하는 경로까지 같이
    확인할 것(이번 advisor 리뷰가 이 구멍을 잡아냈음, 1차 수정만으로는
    안 끝났을 뻔).
  - 1번(그래프 밀도) 튜닝은 "안정화 실패 없음"만 정량 검증했고, "보기
    편한 정도"는 사용자가 실제 그래프를 봐야 판단 가능 — 후속 피드백
    오면 `graphview.py:1244` 부근 `gravitationalConstant`/`springLength`
    재조정.
